# [022] OKR Domain Phase 2 — Junction/贡献表 + habits.key_result_id 迁移

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建 `contributions` junction 表（OKR 私有）消除 habits→key_results 直连 FK，引入读时重算 currentValue 机制（Task Context Provider）。

**Architecture:** contributions 是 OKR 域私有 junction 表（多态 contributorType: task/habit/manual），KR.currentValue 改为从 contributions + 来源状态全量重推导（幂等）。Task 域暴露 CompletedTasksProvider（无 OKR 知识），OKR 域经 resolveContext 拉取完成态数据后重算。

**Tech Stack:** Drizzle ORM, PostgreSQL, TypeScript 5, vitest, hand-written SQL migrations

**Design Spec:** `docs/superpowers/specs/2026-06-25-okr-task-domain-boundary-design.md`（office-hours 产出，SSOT）

**Phase 1 Context:** `docs/superpowers/plans/2026-06-25-022-okr-phase1.md`（已完成：cycles 表 + cycleId + mutation-service）

## Global Constraints

- Tier-2 文档先行（宪法 §IV）：`docs/usom-design.md` + `docs/database-design.md` → 再代码
- Repository Pattern（R-01~R-04）：DB 访问仅经 Repository
- Multi-Tenancy（T-02）：所有查询必须带 userId 过滤
- 手写 SQL 迁移（db:generate/migrate 不可用）：`NNNN_name.sql` + `_journal.json` + psql
- `MVP_USER_ID = '00000000-0000-0000-0000-000000000001'`（硬编码于 `app/actions/okr.ts:23`）
- tsc 零新增、vitest okrs 全绿、grep 守卫（habits domain 内 `keyResultId` 零残留）
- Task Domain 源码对 OKR 概念零引用（仅允许 compliance 测试）
- 注释和文档使用简体中文

---

### Task 1: Tier-2 文档先行 — Contribution 对象 + contributions 表

**Files:**
- Modify: `docs/usom-design.md`（新增 §3.5b Contribution 类型）
- Modify: `docs/database-design.md`（新增 §4.4 contributions 表）
- Modify: `docs/usom-design.md`（§3.4 Habit 移除 keyResultId）
- Modify: `docs/usom-design.md`（§3.5 KeyResult 标注 currentValue 为派生）

**Interfaces:**
- Produces: `Contribution` USOM 类型（后续 Task 2~8 消费）
- Produces: `contributions` 表定义（后续 Task 3 消费）

- [ ] **Step 1: 在 `docs/usom-design.md` 新增 §3.5b Contribution 类型**

在 §3.5a Cycle 之后插入：

```markdown
### 3.5b OKR：Contribution（KR 贡献记录）

**对象意图**：记录外部对象（Task/Habit/手动等）对 KeyResult 进度的贡献，是 OKR 域私有的多态 junction。
OKR 域不感知来源对象的内部结构，仅持有不透明引用（contributorType + contributorId）。

```typescript
export interface Contribution {
  id:              USOM_ID
  keyResultId:     USOM_ID               // 归属的 KR
  contributorType: 'task' | 'habit' | 'manual'
  contributorId:   USOM_ID               // 来源对象 ID（对 OKR 不透明）
  delta?:          number                // 单条贡献增量（非任务数单位时，链接时设定）
  weight?:         number                // 可选权重（默认均等）
  createdAt:       Timestamp
  updatedAt:       Timestamp
}
```

> **设计说明**：`currentValue` 由 `ContributionRepository.recomputeProgress` 读时全量重推导（遍历 contributions + 查询来源状态），天然幂等。
```

- [ ] **Step 2: 更新 `docs/usom-design.md` §3.4 Habit 移除 keyResultId**

找到 Habit 接口定义（约 line 420-430），删除 `keyResultId?: USOM_ID` 行：

```diff
-  keyResultId?: USOM_ID
```

并在变更记录追加：
```markdown
- **2026_06_26 (refactor)**：[022] Phase 2 — Habit 移除 `keyResultId`（迁移至 Contribution junction）；新增 §3.5b `Contribution` 类型
```

- [ ] **Step 3: 更新 `docs/usom-design.md` §3.5 KeyResult 标注 currentValue 为派生**

在 KeyResult 接口的 `currentValue` 行追加注释：

```typescript
  currentValue: number   // 派生：由 ContributionRepository.recomputeProgress 读时重算
```

- [ ] **Step 4: 在 `docs/database-design.md` 新增 §4.4 contributions 表**

在 §4.3 key_results 之后插入：

```markdown
### 4.4 contributions（KR 贡献记录）

OKR 域私有 junction 表。一条记录 = 一个外部对象对 KR 的一次贡献链接。

```sql
CREATE TABLE contributions (
  id               uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id          uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  schema_version   integer NOT NULL DEFAULT 1,
  key_result_id    uuid NOT NULL REFERENCES key_results(id) ON DELETE CASCADE,
  contributor_type text NOT NULL CHECK (contributor_type IN ('task', 'habit', 'manual')),
  contributor_id   uuid NOT NULL,
  delta            numeric(10,2),
  weight           numeric(3,2) DEFAULT 1.0,
  created_at       timestamptz NOT NULL DEFAULT now(),
  updated_at       timestamptz NOT NULL DEFAULT now(),
  UNIQUE(key_result_id, contributor_type, contributor_id)
);

CREATE INDEX idx_contributions_kr ON contributions(user_id, key_result_id);
CREATE INDEX idx_contributions_source ON contributions(contributor_type, contributor_id);
```
```

- [ ] **Step 5: 更新 `docs/database-design.md` §4.2 habits 表**

删除 `key_result_id` 列和 `idx_habits_key_result` 索引（标注为 Phase 2 移除）。

- [ ] **Step 6: 更新 `docs/database-design.md` 表总览**

在表总览表中新增 `contributions ← OKR 贡献记录（junction）`。

- [ ] **Step 7: Commit**

```bash
git add docs/usom-design.md docs/database-design.md
git commit -m "docs(okrs): [022] 2A-T1 Tier-2 文档先行 — Contribution 类型 + contributions 表 + Habit 去 keyResultId

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 2: USOM Contribution 类型 + Habit 去 keyResultId

**Files:**
- Create: `frontend/src/usom/types/contribution.ts`（或合并到 `objects.ts`）
- Modify: `frontend/src/usom/types/objects.ts`（Habit 接口移除 keyResultId）
- Modify: `frontend/src/usom/interfaces/irepository.ts`（新增 IContributionRepository；CreateHabitInput/UpdateHabitInput 移除 keyResultId）

**Interfaces:**
- Consumes: Contribution 类型定义（来自 Task 1 文档）
- Produces: `Contribution` type, `IContributionRepository` interface
- Produces: `CreateHabitInput` / `UpdateHabitInput` 去 keyResultId（Task 7 消费）

- [ ] **Step 1: 在 `objects.ts` 中新增 Contribution 接口**

```typescript
/**
 * KR 贡献记录（OKR 域私有 junction）
 * 
 * 记录外部对象对 KeyResult 进度的贡献链接。
 * contributorType + contributorId 为不透明引用，OKR 不感知来源内部结构。
 */
export interface Contribution {
  id: USOM_ID
  keyResultId: USOM_ID
  contributorType: 'task' | 'habit' | 'manual'
  contributorId: USOM_ID
  delta?: number
  weight?: number
  createdAt: Timestamp
  updatedAt: Timestamp
}
```

- [ ] **Step 2: Habit 接口移除 keyResultId**

```diff
-  keyResultId?: USOM_ID
```

- [ ] **Step 3: 在 `irepository.ts` 中新增 IContributionRepository 接口**

```typescript
export interface IContributionRepository {
  findByKeyResult(keyResultId: USOM_ID, userId: USOM_ID, tx?: DbClient): Promise<Contribution[]>
  findByContributor(contributorType: string, contributorId: USOM_ID, userId: USOM_ID, tx?: DbClient): Promise<Contribution[]>
  add(input: CreateContributionInput, userId: USOM_ID, tx?: DbClient): Promise<Contribution>
  remove(id: USOM_ID, userId: USOM_ID, tx?: DbClient): Promise<void>
  removeByContributor(contributorType: string, contributorId: USOM_ID, userId: USOM_ID, tx?: DbClient): Promise<void>
  recomputeProgress(keyResultId: USOM_ID, userId: USOM_ID, tx?: DbClient): Promise<{ currentValue: number; progressRate: number; completedCount: number; totalCount: number }>
}

export interface CreateContributionInput {
  keyResultId: USOM_ID
  contributorType: 'task' | 'habit' | 'manual'
  contributorId: USOM_ID
  delta?: number
  weight?: number
}
```

- [ ] **Step 4: CreateHabitInput / UpdateHabitInput 移除 keyResultId**

```diff
-  keyResultId?: USOM_ID
```

- [ ] **Step 5: Commit**

```bash
git add frontend/src/usom/
git commit -m "feat(usom): [022] 2A-T2 Contribution 类型 + Habit 去 keyResultId + IContributionRepository

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 3: contributions 表 Schema + 迁移

**Files:**
- Modify: `frontend/src/lib/db/schema.ts`（新增 contributions 表；habits 表移除 keyResultId 列 + 索引）
- Create: `frontend/src/lib/db/migrations/0020_contributions.sql`
- Modify: `frontend/src/lib/db/migrations/meta/_journal.json`

**Interfaces:**
- Consumes: contributions 表定义（来自 Task 1 文档）
- Produces: `contributions` pgTable（Task 4~6 消费）
- Produces: habits 表去 keyResultId（Task 7 消费）

- [ ] **Step 1: 在 `schema.ts` 中新增 contributions 表**

在 key_results 表定义之后：

```typescript
// ─── 4.4 contributions（KR 贡献记录）────────────────────────────
export const contributions = pgTable('contributions', {
  id: uuid('id').primaryKey().defaultRandom(),
  userId: uuid('user_id').notNull().references(() => users.id, { onDelete: 'cascade' }),
  schemaVersion: integer('schema_version').notNull().default(1),
  keyResultId: uuid('key_result_id').notNull().references(() => keyResults.id, { onDelete: 'cascade' }),
  contributorType: text('contributor_type', { enum: ['task', 'habit', 'manual'] }).notNull(),
  contributorId: uuid('contributor_id').notNull(),
  delta: numeric('delta', { precision: 10, scale: 2 }),
  weight: numeric('weight', { precision: 3, scale: 2 }).default('1.0'),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).notNull().defaultNow(),
}, (table) => [
  uniqueIndex('uq_contributions_kr_source').on(table.keyResultId, table.contributorType, table.contributorId),
  index('idx_contributions_kr').on(table.userId, table.keyResultId),
  index('idx_contributions_source').on(table.contributorType, table.contributorId),
])
```

- [ ] **Step 2: habits 表移除 keyResultId 列 + 索引**

```diff
-  keyResultId: uuid('key_result_id').references(() => keyResults.id, { onDelete: 'set null' }),
```

在表配置中删除：
```diff
-  index('idx_habits_key_result').on(table.keyResultId),
```

- [ ] **Step 3: 创建迁移 SQL 文件 `0020_contributions.sql`**

```sql
-- [022] Phase 2：contributions 表 + habits.key_result_id 迁移
-- 1. 建 contributions 表
CREATE TABLE IF NOT EXISTS contributions (
  id               uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id          uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  schema_version   integer NOT NULL DEFAULT 1,
  key_result_id    uuid NOT NULL REFERENCES key_results(id) ON DELETE CASCADE,
  contributor_type text NOT NULL CHECK (contributor_type IN ('task', 'habit', 'manual')),
  contributor_id   uuid NOT NULL,
  delta            numeric(10,2),
  weight           numeric(3,2) DEFAULT 1.0,
  created_at       timestamptz NOT NULL DEFAULT now(),
  updated_at       timestamptz NOT NULL DEFAULT now(),
  CONSTRAINT uq_contributions_kr_source UNIQUE(key_result_id, contributor_type, contributor_id)
);

CREATE INDEX IF NOT EXISTS idx_contributions_kr ON contributions(user_id, key_result_id);
CREATE INDEX IF NOT EXISTS idx_contributions_source ON contributions(contributor_type, contributor_id);

-- 2. 迁移 habits.key_result_id → contributions
INSERT INTO contributions (user_id, key_result_id, contributor_type, contributor_id)
SELECT user_id, key_result_id, 'habit', id
FROM habits
WHERE key_result_id IS NOT NULL
ON CONFLICT (key_result_id, contributor_type, contributor_id) DO NOTHING;

-- 3. 验证迁移完整性
DO $$
DECLARE
  orphan_count integer;
BEGIN
  SELECT COUNT(*) INTO orphan_count
  FROM habits h
  WHERE h.key_result_id IS NOT NULL
    AND NOT EXISTS (
      SELECT 1 FROM contributions c
      WHERE c.key_result_id = h.key_result_id
        AND c.contributor_type = 'habit'
        AND c.contributor_id = h.id
    );
  IF orphan_count > 0 THEN
    RAISE WARNING 'orphan habits with key_result_id not in contributions: %', orphan_count;
  END IF;
END $$;

-- 4. 删除 habits.key_result_id 列
DROP INDEX IF EXISTS idx_habits_key_result;
ALTER TABLE habits DROP CONSTRAINT IF EXISTS habits_key_result_id_fkey;
ALTER TABLE habits DROP COLUMN IF EXISTS key_result_id;
```

- [ ] **Step 4: 登记 journal**

```json
{"idx": 20, "version": "20", "when": "2026-06-26T00:00:00.000Z", "tag": "022-phase2-contributions"}
```

- [ ] **Step 5: 执行迁移并验证**

```bash
docker exec lifeware-postgres-1 psql -U lifeware_dev -d lifeware_dev -v ON_ERROR_STOP=1 -f frontend/src/lib/db/migrations/0020_contributions.sql
```

验证：
```sql
-- contributions 表存在
SELECT to_regclass('public.contributions');
-- habits 表无 key_result_id 列
SELECT column_name FROM information_schema.columns WHERE table_name = 'habits' AND column_name = 'key_result_id';
-- 已迁移数据量
SELECT COUNT(*) FROM contributions WHERE contributor_type = 'habit';
```

- [ ] **Step 6: Commit**

```bash
git add frontend/src/lib/db/schema.ts frontend/src/lib/db/migrations/
git commit -m "feat(okrs): [022] 2A-T3 contributions 表 + habits.key_result_id 迁移

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 4: ContributionRepository 实现

**Files:**
- Create: `frontend/src/domains/okrs/repository/contribution.ts`
- Create: `frontend/src/domains/okrs/__tests__/contribution-repo.test.ts`

**Interfaces:**
- Consumes: `IContributionRepository`（Task 2）, `contributions` pgTable（Task 3）
- Produces: `ContributionRepository` class（Task 5~6 消费）

- [ ] **Step 1: 写失败测试**

```typescript
/**
 * @file contribution-repo.test
 * @brief ContributionRepository 集成测试
 */
import { describe, it, expect, beforeAll, afterAll } from 'vitest'
import { ContributionRepository } from '../repository/contribution'
import { KeyResultRepository } from '../repository/key-result'
import { ObjectiveRepository } from '../repository/objective'
import { db } from '@/lib/db'

const USER_ID = '00000000-0000-0000-0000-000000000001' // 与 okr.ts 保持一致

describe('ContributionRepository', () => {
  const repo = new ContributionRepository()

  it('add → findByKeyResult 往返', async () => {
    const c = await repo.add({
      keyResultId: 'test-kr-id',
      contributorType: 'task',
      contributorId: 'test-task-id',
    }, USER_ID)
    expect(c.id).toBeDefined()
    expect(c.contributorType).toBe('task')

    const found = await repo.findByKeyResult('test-kr-id', USER_ID)
    expect(found.length).toBeGreaterThanOrEqual(1)
    expect(found.some(f => f.id === c.id)).toBe(true)
  })

  it('findByContributor 按来源过滤', async () => {
    const found = await repo.findByContributor('task', 'test-task-id', USER_ID)
    expect(found.length).toBeGreaterThanOrEqual(1)
  })

  it('remove 后查不到', async () => {
    const c = await repo.add({
      keyResultId: 'test-kr-id-2',
      contributorType: 'manual',
      contributorId: 'test-manual-id',
    }, USER_ID)
    await repo.remove(c.id, USER_ID)
    const found = await repo.findByKeyResult('test-kr-id-2', USER_ID)
    expect(found.some(f => f.id === c.id)).toBe(false)
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

```bash
cd frontend && npx vitest run src/domains/okrs/__tests__/contribution-repo.test.ts
```
Expected: FAIL（ContributionRepository 不存在）

- [ ] **Step 3: 实现 ContributionRepository**

```typescript
/**
 * @file contribution
 * @brief Contribution 仓储实现（OKR 域私有 junction）
 *
 * [022] Phase 2：CRUD + 按 KR/来源查询 + recomputeProgress 读时重算。
 */

import { eq, and } from 'drizzle-orm'
import { db, type DbClient } from '../../../lib/db/index'
import * as s from '../../../lib/db/schema'
import type { IContributionRepository, CreateContributionInput } from '../../../usom/interfaces/irepository'
import type { Contribution } from '../../../usom/types/objects'
import type { USOM_ID, Timestamp } from '../../../usom/types/primitives'

export class ContributionRepository implements IContributionRepository {
  async findByKeyResult(keyResultId: USOM_ID, userId: USOM_ID, tx: DbClient = db): Promise<Contribution[]> {
    const rows = await tx.select().from(s.contributions)
      .where(and(
        eq(s.contributions.keyResultId, keyResultId),
        eq(s.contributions.userId, userId),
      ))
    return rows.map(r => contributionRowToUSOM(r))
  }

  async findByContributor(
    contributorType: string,
    contributorId: USOM_ID,
    userId: USOM_ID,
    tx: DbClient = db,
  ): Promise<Contribution[]> {
    const rows = await tx.select().from(s.contributions)
      .where(and(
        eq(s.contributions.contributorType, contributorType),
        eq(s.contributions.contributorId, contributorId),
        eq(s.contributions.userId, userId),
      ))
    return rows.map(r => contributionRowToUSOM(r))
  }

  async add(input: CreateContributionInput, userId: USOM_ID, tx: DbClient = db): Promise<Contribution> {
    const now = new Date().toISOString() as Timestamp
    const id = crypto.randomUUID() as USOM_ID
    await tx.insert(s.contributions).values({
      id,
      userId,
      keyResultId: input.keyResultId,
      contributorType: input.contributorType,
      contributorId: input.contributorId,
      delta: input.delta?.toString(),
      weight: input.weight?.toString() ?? '1.0',
      createdAt: new Date(now),
      updatedAt: new Date(now),
    } as any)
    return {
      id,
      keyResultId: input.keyResultId,
      contributorType: input.contributorType,
      contributorId: input.contributorId,
      delta: input.delta,
      weight: input.weight ?? 1.0,
      createdAt: now,
      updatedAt: now,
    }
  }

  async remove(id: USOM_ID, userId: USOM_ID, tx: DbClient = db): Promise<void> {
    await tx.delete(s.contributions)
      .where(and(eq(s.contributions.id, id), eq(s.contributions.userId, userId)))
  }

  async removeByContributor(
    contributorType: string,
    contributorId: USOM_ID,
    userId: USOM_ID,
    tx: DbClient = db,
  ): Promise<void> {
    await tx.delete(s.contributions)
      .where(and(
        eq(s.contributions.contributorType, contributorType),
        eq(s.contributions.contributorId, contributorId),
        eq(s.contributions.userId, userId),
      ))
  }

  async recomputeProgress(
    keyResultId: USOM_ID,
    userId: USOM_ID,
    tx: DbClient = db,
  ): Promise<{ currentValue: number; progressRate: number; completedCount: number; totalCount: number }> {
    const contributions = await this.findByKeyResult(keyResultId, userId, tx)
    // ... 读时重算逻辑（Task 6 补全，目前返回占位）
    return { currentValue: 0, progressRate: 0, completedCount: 0, totalCount: contributions.length }
  }
}

/** contribution 行 → USOM */
function contributionRowToUSOM(row: Record<string, unknown>): Contribution {
  return {
    id: row.id as USOM_ID,
    keyResultId: row.keyResultId as USOM_ID,
    contributorType: row.contributorType as Contribution['contributorType'],
    contributorId: row.contributorId as USOM_ID,
    delta: row.delta != null ? Number(row.delta) : undefined,
    weight: row.weight != null ? Number(row.weight) : 1.0,
    createdAt: (row.createdAt as Date).toISOString() as Timestamp,
    updatedAt: (row.updatedAt as Date).toISOString() as Timestamp,
  }
}
```

- [ ] **Step 4: 运行测试确认通过**

```bash
cd frontend && npx vitest run src/domains/okrs/__tests__/contribution-repo.test.ts
```
Expected: 3/3 PASS

- [ ] **Step 5: Commit**

```bash
git add frontend/src/domains/okrs/repository/contribution.ts frontend/src/domains/okrs/__tests__/contribution-repo.test.ts
git commit -m "feat(okrs): [022] 2A-T4 ContributionRepository（CRUD + 骨架 recomputeProgress）

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 5: Task Domain — CompletedTasksProvider

**Files:**
- Create: `frontend/src/domains/tasks/providers/completed-tasks-provider.ts`
- Modify: `frontend/src/domains/tasks/providers/index.ts`（导出）
- Modify: `frontend/src/nexus/context-engine/register-providers.ts`（注册）

**Interfaces:**
- Consumes: `ITaskRepository`（已有）、`ContextProvider`（已有）
- Produces: `CompletedTasksProvider` class（Task 6 recomputeProgress 消费）

- [ ] **Step 1: 创建 CompletedTasksProvider**

```typescript
/**
 * @file completed-tasks-provider
 * @brief 已完成任务上下文提供者
 * 
 * 实现 ContextProvider 接口，提供已完成任务数据的查询能力。
 * 无 OKR 知识——仅暴露任务完成态信息，供他域（如 OKR）读时聚合。
 */

import type { ContextProvider } from '@/usom/types/process'
import type { ITaskRepository } from '@/usom/interfaces/irepository'
import type { USOM_ID } from '@/usom/types/primitives'

export class CompletedTasksProvider implements ContextProvider {
  constructor(private readonly repo: ITaskRepository) {}

  async provide(query: string, params: Record<string, unknown>): Promise<unknown> {
    if (query !== 'completed_ids') return []

    const { userId } = params as { userId: USOM_ID }
    const tasks = await this.repo.findByStatuses(['completed'], userId)

    return tasks.map(t => ({
      id: t.id,
      title: t.title,
      completedAt: t.completedAt,
    }))
  }
}
```

- [ ] **Step 2: 更新 providers/index.ts 导出**

```typescript
export { CompletedTasksProvider } from './completed-tasks-provider'
```

- [ ] **Step 3: 在 register-providers.ts 注册**

```typescript
import { CompletedTasksProvider } from '@/domains/tasks/providers'

const CompletedTaskArraySchema = z.array(z.object({
  id: z.string(),
  title: z.string(),
  completedAt: z.string().optional(),
}))

// 在 registerAllProviders 内 taskRepo 分支追加:
registerContextCapability({
  id: 'completedTasks',
  visibility: 'planning',
  schema: CompletedTaskArraySchema,
  description: '已完成任务（供跨域贡献重算）',
  provider: new CompletedTasksProvider(deps.taskRepo),
})
```

- [ ] **Step 4: Commit**

```bash
git add frontend/src/domains/tasks/providers/ frontend/src/nexus/context-engine/register-providers.ts
git commit -m "feat(tasks): [022] 2A-T5 CompletedTasksProvider（供 OKR 贡献重算）

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 6: ContributionRepository.recomputeProgress 完整实现

**Files:**
- Modify: `frontend/src/domains/okrs/repository/contribution.ts`（补全 recomputeProgress）
- Modify: `frontend/src/domains/okrs/__tests__/contribution-repo.test.ts`（加重算测试）

**Interfaces:**
- Consumes: `CompletedTasksProvider`（Task 5）、`resolveContext`（nexus context-engine）
- Produces: `recomputeProgress` 完整实现（Task 8 消费）

- [ ] **Step 1: 加重算测试用例**

```typescript
it('recomputeProgress 任务数单位 — 统计已完成 task 贡献', async () => {
  // 准备：创建 KR（unit='任务数'） + 两个 task 贡献
  // ...
  const result = await repo.recomputeProgress(krId, USER_ID)
  expect(result.completedCount).toBeGreaterThanOrEqual(0)
  expect(result.currentValue).toBe(result.completedCount) // 任务数 = 完成数
})

it('recomputeProgress 非任务数单位 — 加总 delta', async () => {
  // 准备：创建 KR（unit='小时'） + 两个贡献各 delta=5
  // ...
  const result = await repo.recomputeProgress(krId, USER_ID)
  expect(result.currentValue).toBe(10)
})
```

- [ ] **Step 2: 运行测试确认失败**

- [ ] **Step 3: 补全 recomputeProgress 实现**

```typescript
import { resolveContext } from '../../../nexus/context-engine'

async recomputeProgress(
  keyResultId: USOM_ID,
  userId: USOM_ID,
  tx: DbClient = db,
): Promise<{ currentValue: number; progressRate: number; completedCount: number; totalCount: number }> {
  // 1. 获取 KR 信息（用于判断 unit）
  const krRows = await tx.select({ unit: s.keyResults.unit, targetValue: s.keyResults.targetValue })
    .from(s.keyResults)
    .where(and(eq(s.keyResults.id, keyResultId), eq(s.keyResults.userId, userId)))
  if (!krRows[0]) throw new Error(`KeyResult ${keyResultId} not found`)
  const kr = krRows[0]
  const targetValue = Number(kr.targetValue)
  const isCountUnit = kr.unit === '任务数'

  // 2. 读取所有贡献记录
  const contributions = await this.findByKeyResult(keyResultId, userId, tx)
  const totalCount = contributions.length

  if (totalCount === 0) {
    return { currentValue: 0, progressRate: 0, completedCount: 0, totalCount: 0 }
  }

  // 3. 按 contributorType 分组查询来源完成状态
  let currentValue = 0
  let completedCount = 0

  // 3a. Task 来源：经 ContextProvider 获取已完成 task ID 集合
  const taskContribs = contributions.filter(c => c.contributorType === 'task')
  if (taskContribs.length > 0) {
    const completedTasks = await resolveContext('completedTasks', 'completed_ids', { userId }) as Array<{ id: string }>
    const completedIds = new Set(completedTasks.map(t => t.id))
    for (const c of taskContribs) {
      if (completedIds.has(c.contributorId)) {
        completedCount++
        currentValue += isCountUnit ? 1 : (c.delta ?? targetValue / totalCount)
      }
    }
  }

  // 3b. Habit 来源：直接查 habit_logs 完成态
  const habitContribs = contributions.filter(c => c.contributorType === 'habit')
  if (habitContribs.length > 0) {
    const habitIds = habitContribs.map(c => c.contributorId)
    const completedHabitLogs = await tx.select({ habitId: s.habitLogs.habitId })
      .from(s.habitLogs)
      .where(and(
        eq(s.habitLogs.userId, userId),
        eq(s.habitLogs.completionStatus, 'completed'),
      ))
    // 有任意完成记录的 habit 视为"已完成"（对 KR 贡献一次）
    const completedHabitIds = new Set(completedHabitLogs.map(l => l.habitId))
    for (const c of habitContribs) {
      if (completedHabitIds.has(c.contributorId)) {
        completedCount++
        currentValue += isCountUnit ? 1 : (c.delta ?? targetValue / totalCount)
      }
    }
  }

  // 3c. Manual 来源：直接视为已完成
  const manualContribs = contributions.filter(c => c.contributorType === 'manual')
  for (const c of manualContribs) {
    completedCount++
    currentValue += isCountUnit ? 1 : (c.delta ?? targetValue / totalCount)
  }

  // 4. 钳制 + 计算进度率
  currentValue = Math.min(currentValue, targetValue)
  const progressRate = targetValue > 0 ? Number((currentValue / targetValue).toFixed(4)) : 0

  return { currentValue, progressRate, completedCount, totalCount }
}
```

- [ ] **Step 4: 运行测试确认通过**

```bash
cd frontend && npx vitest run src/domains/okrs/__tests__/contribution-repo.test.ts
```
Expected: 5/5 PASS

- [ ] **Step 5: Commit**

```bash
git add frontend/src/domains/okrs/repository/contribution.ts frontend/src/domains/okrs/__tests__/contribution-repo.test.ts
git commit -m "feat(okrs): [022] 2B-T6 recomputeProgress 完整实现（ContextProvider + habit_logs + manual）

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 7: Habit 域去 keyResultId（Repository + Mapper + Manifest + 涟漪修复）

**Files:**
- Modify: `frontend/src/domains/habits/repository/habit.ts`（create/update 去 keyResultId）
- Modify: `frontend/src/lib/db/repositories/mappers.ts`（habitRowToUSOM/habitUSOMToRow 去 keyResultId；HabitRow type 去 keyResultId 字段）
- Modify: `frontend/src/domains/habits/manifest.yaml`（field_metadata 删 keyResultId）
- Modify: `frontend/src/domains/habits/__tests__/manifest-field-metadata.test.ts`（更新断言）
- Modify: `frontend/src/usom/types/objects.ts`（Habit 接口去 keyResultId — 如 Task 2 未做）
- Modify: `frontend/src/usom/types/summaries.ts`（HabitSummary 去 keyResultId）

**Interfaces:**
- Consumes: Habit 接口（去 keyResultId）、schema 列已删（Task 3）
- Produces: 零 `keyResultId` 残留的 habits 域

- [ ] **Step 1: habit.ts — create 方法去 keyResultId**

```diff
-      keyResultId: data.keyResultId,
```

- [ ] **Step 2: habit.ts — update 方法去 keyResultId**

```diff
-      ...(data.keyResultId !== undefined && { keyResultId: data.keyResultId }),
```

- [ ] **Step 3: mappers.ts — habitRowToUSOM 去 keyResultId**

```diff
-    keyResultId: row.keyResultId ?? undefined,
```

- [ ] **Step 4: mappers.ts — habitUSOMToRow 去 keyResultId**

```diff
-    keyResultId: habit.keyResultId ?? null,
```

- [ ] **Step 5: manifest.yaml — field_metadata 删 keyResultId 条目**

```diff
-  keyResultId:
-    type: string
-    mutation_mode: ContentField
```

- [ ] **Step 6: `summaries.ts` — HabitSummary 去 keyResultId**

```diff
-  keyResultId?: USOM_ID
```

- [ ] **Step 7: manifest-field-metadata.test.ts 更新断言**

```diff
-    'title', 'description', 'startDate', 'endDate', 'keyResultId', 'tags',
+    'title', 'description', 'startDate', 'endDate', 'tags',
```

- [ ] **Step 8: 全仓 grep 守卫**

```bash
grep -rn "keyResultId\|key_result_id" frontend/src/domains/habits/ --include="*.ts" --include="*.tsx" --include="*.yaml" | grep -v __tests__ | grep -v node_modules
```
Expected: 零输出（或仅测试文件中的历史引用）

- [ ] **Step 9: Commit**

```bash
git add frontend/src/domains/habits/ frontend/src/lib/db/repositories/mappers.ts frontend/src/usom/types/objects.ts
git commit -m "refactor(habits): [022] 2C-T7 Habit 域去 keyResultId（Repository + Mapper + Manifest）

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 8: KeyResultRepository.recomputeProgress 集成 + 旧 updateProgress 替换

**Files:**
- Modify: `frontend/src/domains/okrs/repository/key-result.ts`（updateProgress → 调用 ContributionRepository.recomputeProgress）
- Modify: `frontend/src/domains/okrs/__tests__/key-result-repo.test.ts`（新增重算集成测试）

**Interfaces:**
- Consumes: `ContributionRepository.recomputeProgress`（Task 6）
- Produces: `KeyResultRepository.updateProgress` 走 junction 重算

- [ ] **Step 1: 写集成测试**

```typescript
it('updateProgress 经 ContributionRepository 重算 currentValue', async () => {
  // 1. 创建 KR（unit='任务数', targetValue=5）
  // 2. 添加 3 个 task 贡献（其中 2 个已完成）
  // 3. 调用 updateProgress → 断言 currentValue=2
})
```

- [ ] **Step 2: refactor updateProgress 使用 recomputeProgress**

```typescript
import { ContributionRepository } from './contribution'

async updateProgress(id: USOM_ID, _currentValue: number, userId: USOM_ID): Promise<KeyResult> {
  const contributionRepo = new ContributionRepository()
  const { currentValue, progressRate } = await contributionRepo.recomputeProgress(id, userId)
  
  await db.update(s.keyResults)
    .set({
      currentValue: String(currentValue),
      progressRate: String(progressRate),
      updatedAt: new Date(),
    })
    .where(and(eq(s.keyResults.id, id), eq(s.keyResults.userId, userId)))

  const updated = await this.findById(id, userId)
  if (!updated) throw new Error(`KeyResult ${id} not found after updateProgress`)
  return updated
}
```

- [ ] **Step 3: 运行测试确认通过**

- [ ] **Step 4: Commit**

```bash
git add frontend/src/domains/okrs/repository/key-result.ts frontend/src/domains/okrs/__tests__/
git commit -m "refactor(okrs): [022] 2B-T8 KeyResultRepository.updateProgress 经 ContributionRepository 重算

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 9: 收尾验证 — manifest.md + tsc + vitest + grep 全量

**Files:**
- Modify: `manifest.md`（版本历史 + T19 行）
- 验证：tsc / vitest / grep 守卫

- [ ] **Step 1: tsc 验证**

```bash
cd frontend && npx tsc --noEmit 2>&1 | grep "^src/" | wc -l
```
Expected: ≤49（零新增）

- [ ] **Step 2: OKR vitest 全量**

```bash
cd frontend && npx vitest run src/domains/okrs/
```
Expected: 全部通过（当前 56，新增 5~8）

- [ ] **Step 3: Habits vitest 全量**

```bash
cd frontend && npx vitest run src/domains/habits/
```
Expected: 全部通过（无回归）

- [ ] **Step 4: grep 守卫**

```bash
# OKR action 无 repo 直写
grep -n "\.save\|\.updateProgress\|\.updateFields" frontend/src/app/actions/okr.ts

# Habits 域零 keyResultId 残留
grep -rn "keyResultId\|key_result_id" frontend/src/domains/habits/ --include="*.ts" --include="*.tsx" --include="*.yaml" | grep -v __tests__

# Task 域零 OKR 概念
grep -rn "keyResult\|objective\|okr" frontend/src/domains/tasks/ --include="*.ts" --include="*.tsx" | grep -v __tests__
```

- [ ] **Step 5: 更新 manifest.md 版本历史**

```markdown
| 2026_06_26 | [022] Phase 2（contributions junction + habits.key_result_id 迁移） | feat/022-okr-domain-reorg | 0.1.0 |
```

- [ ] **Step 6: Commit**

```bash
git add manifest.md
git commit -m "docs(okrs): [022] 2C-T9 Phase 2 收尾验证 + manifest 版本历史

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## 依赖图

```
T1 (Tier-2 文档)
 └─ T2 (USOM 类型 + IContributionRepository)
      ├─ T3 (Schema + 迁移)
      │    └─ T4 (ContributionRepository CRUD)
      │         └─ T6 (recomputeProgress 完整实现)
      │              └─ T8 (KeyResultRepository 集成)
      ├─ T5 (CompletedTasksProvider) ── 并行于 T3/T4 ──┘
      └─ T7 (Habit 去 keyResultId) ── 并行于 T4/T5/T6 ──┘
           └─ T9 (收尾验证)
```

T1→T2→T3→T4→T6→T8 为主链；T5（Task Provider）和 T7（Habit 清理）可与主链并行。
