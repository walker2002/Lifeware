# [026.02.3.1] 4 项 fresh drift 修复 + 5 cosmetic minor 收口 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 /lifeware-neat (2026-07-09) 重扫发现的 4 项 fresh drift (TD-024/025/026/027) + 收口 [026.02.2] whole-branch review 5 cosmetic minor polish, 1 PR ship-ready。

**Architecture:** 6 task 串行 — T1 USOM/DB/code 同步扩 6 值, T2 view 重写 (含 0036 migration), T3 docs 格式统一 (Markdown table → SQL block), T4 docs 页脚 bump, T5 5 cosmetic minor polish (1:1 替换), T6 CHANGELOG + manifest 段。

**Tech Stack:** TypeScript 5, Drizzle ORM 0.45.1, Vitest 4.1.5, vitest-globals (pre-existing TS2304 baseline), PostgreSQL (convention: 手写 migration + journal, 不跑 drizzle-kit up)。

## Global Constraints

- 宪章: `.specify/memory/constitution.md` v2.1.1
  - R-01~R-04 (Repository 模式)
  - T-01~T-04 (Multi-Tenancy — 所有 query/write 必带 user_id)
  - A~D (Bridge Layer — MVP 仅约束)
  - G-01~G-08 (USOM Governance)
- tier-2 文档强制同步: USOM 变更 MUST 同步 `docs/usom-design.md` + `docs/database-design.md` + `frontend/src/lib/db/schema.ts` + `CHANGELOG.md`
- pre-push hooks (强制过):
  - `npm run validate:manifest` (0 errors, 2 WARN + 2 INFO pre-existing 是基线)
  - `npm run validate:structure` (全部通过)
  - `npm run validate:rules-registry` (6 项 lifecycle 一致)
- Drizzle migrations: 项目不使用 `drizzle-kit up`, 迁移一律手写 SQL + 登记 journal (沿用 [023] F2 convention)
- 测试 IRON RULE baseline: vitest 47 pre-existing failures (与本任务无关); tsc 95 pre-existing errors; 改动文件 target = 0 new
- 工作流位置: repo root → `cd frontend` (npm/vitest 命令)
- 规则: 「Doctrine of surprising the user — never」; YAGNI; TDD; frequent commits
- 已有 [026.02] / [026.02.1] / [026.02.2] / [026.02.3] 已 ship 基础:
  - `fix(026.02.3): /editAppointment selecting→编辑视图 TypeError 双层防御` (e97b9a4)
  - `docs(026.02.3): CHANGELOG 段` (3ffc2c9)
  - `docs(manifest): 补 [026.02.3] 入口` (c220e15)
- IRON RULE 历史守卫: mini-calendar.regression + register-client-surfaces 8/8 green (本任务不动)

---

## File Structure（修改前先锁）

| 文件 | 变更类型 | 用途 | Task |
|---|---|---|---|
| `frontend/src/usom/types/primitives.ts` | modify line 230-235 | USOM `AISessionStatus` 枚举 | T1 |
| `frontend/src/nexus/ai-runtime/session/index.ts` | modify line 1-10 + 5 call sites | 删 `SessionStatus` 局部别名,改 import USOM | T1 |
| `docs/database-design.md` | multiple sites | T2 view, T3 ai_sessions SQL, T4 footer | T2/T3/T4 |
| `frontend/src/lib/db/migrations/0036_drop_v_running_timeboxes_recreate.sql` | create | view 重写 migration + journal | T2 |
| `docs/superpowers/journal/drizzle-journal.md` (or equivalent) | modify | 登记 idx=36 | T2 |
| `docs/usom-design.md` | line 1690 + 末尾 footer | T4 footer bump | T4 |
| `CHANGELOG.md` | add `## [026.02.3.1]` 段 + 5 cosmetic minor 数字修正 | T5 + T6 | T5/T6 |
| `docs/superpowers/specs/2026-07-09-026-02-2-appointment-polish-design.md` | modify C2 (test count) | T5 |
| `docs/superpowers/plans/2026-07-09-026-02-2-appointment-polish.md` | modify C2 | T5 |
| `frontend/src/domains/timebox/components/__tests__/appointment-workspace.test.tsx` | modify line 294 | T5 mock shape | T5 |
| `frontend/src/domains/timebox/lib/__tests__/appointment-filter.test.ts` | mk() comment | T5 | T5 |
| `manifest.md` | add `# [026.02.3.1]` 入口 | T6 | T6 |
| `frontend/src/nexus/ai-runtime/__tests__/session-status.test.ts` | create (T1 IRON RULE) | T1 |
| `docs/tech-debt/TD-024..TD-027-*.md` | create 4 instances + README index | SDD ship 后 | Ship (final) |

---

### Task 1: TD-024 AISessionStatus USOM 3→6 值与 DB/code 对齐

**Files:**
- Modify: `frontend/src/usom/types/primitives.ts:230-235`
- Modify: `frontend/src/nexus/ai-runtime/session/index.ts:1-10` + 5 call sites
- Modify: `docs/database-design.md:1710` (no functional change, comment align)
- Test: `frontend/src/nexus/ai-runtime/__tests__/session-status.test.ts` (new IRON RULE)

**Interfaces:**
- Consumes: `frontend/src/lib/db/schema.ts:619` (`enum: ['created', 'active', 'completing', 'archived', 'closed', 'deleted']`)
- Produces: `AISessionStatus = 'created' | 'active' | 'completing' | 'archived' | 'closed' | 'deleted'` (6 values); `session/index.ts` 删局部别名,改 import USOM

- [ ] **Step 1: 写失败 IRON RULE 测试 — 锁定 USOM 6 值 shape**

`frontend/src/nexus/ai-runtime/__tests__/session-status.test.ts` (new file):

```ts
/**
 * @file session-status.test
 * @brief [026.02.3.1] T1 IRON RULE — AISessionStatus 必须 6 值与 DB schema 对齐
 *
 * 守护对象：USOM `AISessionStatus` 6 值 vs DB schema 6 值 vs 实际代码 5 值
 * (其中 `'deleted'` USOM 漏, `'created'`/`'completing'`/`'closed'` 漏)。
 * 本测试用 type-level + runtime array literal 双重校验。
 */
import { describe, it, expect } from 'vitest'
import type { AISessionStatus } from '@/usom/types/primitives'

// 真实运行时的 6 值 const 数组 - 必须严格匹配 USOM union
const EXPECTED_AI_SESSION_STATUSES = [
  'created',
  'active',
  'completing',
  'archived',
  'closed',
  'deleted',
] as const satisfies readonly AISessionStatus[]

describe('[026.02.3.1] T1 IRON RULE — AISessionStatus shape', () => {
  it('USOM AISessionStatus 必须含 6 值 (created/active/completing/archived/closed/deleted)', () => {
    // type-level: as const satisfies 若不匹配会编译错
    // runtime-level: 数组相等
    expect(EXPECTED_AI_SESSION_STATUSES).toHaveLength(6)
    expect(EXPECTED_AI_SESSION_STATUSES).toContain('created')
    expect(EXPECTED_AI_SESSION_STATUSES).toContain('active')
    expect(EXPECTED_AI_SESSION_STATUSES).toContain('completing')
    expect(EXPECTED_AI_SESSION_STATUSES).toContain('archived')
    expect(EXPECTED_AI_SESSION_STATUSES).toContain('closed')
    expect(EXPECTED_AI_SESSION_STATUSES).toContain('deleted')
  })
})
```

- [ ] **Step 2: 运行测试, 确认失败**

Run: `cd frontend && npx vitest run src/nexus/ai-runtime/__tests__/session-status.test.ts`
Expected: **FAIL** with TS error `Type '"created"' is not assignable to type 'AISessionStatus'` (因为现 USOM 是 3 值 `'active' | 'archived' | 'deleted'`)

- [ ] **Step 3: 扩展 USOM `AISessionStatus` 到 6 值**

`frontend/src/usom/types/primitives.ts:230-237`:

```ts
/**
 * AI 会话状态
 *
 * [026.02.3.1] T1 三向一致: USOM 6 值 = schema.ts ai_sessions enum 6 值 =
 * database-design.md ENUM(created/active/completing/archived/closed/deleted) 6 值。
 * 旧版 USOM 漏 3 值: 'created'(初次创建)、'completing'(完成中 — 由 active 推进)、'closed'(user 主动关闭)。
 * 终态 2 种语义区分: 'closed'(user 主行为终止) vs 'deleted'(软删 — 当前无 server action, 留字段兼容未来)。
 *
 * - created: 初创
 * - active: 活跃中
 * - completing: 完成中（用户提提交后、归档前）
 * - archived: 已归档
 * - closed: 已关闭（用户主动结束会话）
 * - deleted: 已软删除
 */
export type AISessionStatus =
  | 'created'
  | 'active'
  | 'completing'
  | 'archived'
  | 'closed'
  | 'deleted'
```

- [ ] **Step 4: 删 `session/index.ts` 局部 `SessionStatus` 别名, 改 import USOM**

`frontend/src/nexus/ai-runtime/session/index.ts:1-15`:

```ts
import type { AISessionStatus } from '@/usom/types/primitives'

interface SessionTransitionMap {
  [K in AISessionStatus]: readonly AISessionStatus[]
}

/**
 * [026.02.3.1] T1 改造: 删局部 `SessionStatus = 'created' | 'active' | 'completing' | 'archived' | 'closed'`
 * 别名,改用 USOM `AISessionStatus`(6 值含 'deleted'); transition 表对应扩展。
 */
const sessionTransitions: SessionTransitionMap = {
  // [023.08] / [026.02.3.1] transition map — 'closed' 是终态, 'deleted' 留字段兼容未来 server action
  created: ['active', 'closed'],
  active: ['completing', 'closed'],
  completing: ['archived', 'closed'],
  archived: [],        // 终态
  closed: [],          // 终态 — 用户主动结束
  deleted: [],         // 终态 — 软删字段（当前无 server action 触发）
}

// 旧 3 call sites (假设已记录 search 结果):
//   - line 37: `created: ['active', 'closed'],` — 已在上方 new map 体现
//   - line 38: `active: ['completing', 'closed'],` — 同上
//   - line 39: `completing: ['archived', 'closed'],` — 同上
//   - line 81: `status: 'created' as SessionStatus` — 改 `as AISessionStatus`
//   - line 93: `return transition(sessionId, 'completing')` — type inferred 仍 ok
//   - line 101: `return transition(sessionId, 'closed')` — 同上
//   任何遗漏的 `SessionStatus` 引用 — 全部改 `AISessionStatus`
```

Run: `cd frontend && grep -rn "SessionStatus" src/nexus/ai-runtime/ | head -20`
Expected: 仅命中 `AISessionStatus` import 行 + transition map 内部字段 (5 处)

- [ ] **Step 5: 运行 IRON RULE 测试, 确认 pass**

Run: `cd frontend && npx vitest run src/nexus/ai-runtime/__tests__/session-status.test.ts`
Expected: **PASS** — 6 值 type-level satisfies + runtime array 校验均过

Run: `cd frontend && npx vitest run src/nexus/ai-runtime/session src/nexus/ai-runtime/__tests__/session-query.test.ts`
Expected: **PASS** (pre-existing 0 回归, 改动 0 错)

Run: `cd frontend && npx tsc --noEmit 2>&1 | grep -E "session/index|primitives.ts:230" | head -5`
Expected: 0 行 (types 已扩展匹配)

- [ ] **Step 6: 同步 docs 与 schema 三向对齐注释 (跨文档 sanity, 1 行)**

`docs/database-design.md:1710` 现有 `ENUM(created/active/completing/archived/deleted/closed)` 已对齐,无需 text 改。仅在该段 §十三 设计决策记录处加一行引用:

Read line 1685-1700 — find 「设计决策记录」section header, 在合适位置加:

```md
- **[026.02.3.1] T1**：USOM `AISessionStatus` 与 DB schema + `session/index.ts` transition map 三向一致（6 值 `created/active/completing/archived/closed/deleted`，现均为 6 值，IRON RULE `session-status.test.ts` 守护）。
```

- [ ] **Step 7: Commit T1**

```bash
cd /home/walker/lifeware
git add frontend/src/usom/types/primitives.ts \
        frontend/src/nexus/ai-runtime/session/index.ts \
        frontend/src/nexus/ai-runtime/__tests__/session-status.test.ts \
        docs/database-design.md
git commit -m "fix(026.02.3.1): T1 AISessionStatus USOM 3→6 值与 DB schema 对齐

[026.02.3.1] /lifeware-neat 重扫发现的 TD-024:
- USOM 'active' | 'archived' | 'deleted' (3) →
  'created' | 'active' | 'completing' | 'archived' | 'closed' | 'deleted' (6)
- 删 session/index.ts 局部 `SessionStatus` 别名,改 import USOM
- 新增 IRON RULE 测试 nexus/ai-runtime/__tests__/session-status.test.ts
  锁定 6 值 shape, 防止后续再次 drift

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 2: TD-025 v_running_timeboxes view 重写 + 0036 migration

**Files:**
- Modify: `docs/database-design.md:1548-1557`
- Create: `frontend/src/lib/db/migrations/0036_drop_v_running_timeboxes_recreate.sql`
- Modify: `frontend/src/lib/db/migrations/journal.md` (or equivalent journal file) — register idx=36
- Modify: `docs/usom-design.md` §3.9 — 增派生语义注释
- Test: 实际 SQL 在 dev DB 跑 (无单测; 已用 jest-style 替代方案)

**Interfaces:**
- Consumes: 现 `timeboxes.status` 是 3 值 (`planned | logged | cancelled`)
- Produces: 1 view SQL 用 `status='planned' AND start_time <= NOW() AND end_time >= NOW()` 派生「运行中」(时间派生态,非持久化 status)

- [ ] **Step 1: 替换 docs/database-design.md view SQL**

`docs/database-design.md:1548-1557`:

现:
```sql
CREATE VIEW v_running_timeboxes AS
SELECT id, user_id, title, status, start_time, end_time, tags
FROM timeboxes
WHERE status IN ('running', 'overtime');
```

替换:
```sql
-- [026.02.3.1] T2 — [023.12] status 6→3 态后 'running'/'overtime' 不再持久化。
-- 重写 view 派生「当前进行中」: planned 且 now 落在 [start_time, end_time] 区间内。
-- 与 derive-display-status.ts 派生语义对齐 (server side 优化版)。
-- 替代状态 'overtime': 是 logged + 已过 end_time 状态用 case when 派生 (不在 view 范围)。
CREATE OR REPLACE VIEW v_running_timeboxes AS
SELECT id, user_id, title, status, start_time, end_time, tags
FROM timeboxes
WHERE status = 'planned'
  AND start_time <= NOW()
  AND end_time >= NOW();
```

- [ ] **Step 2: 创建 0036 migration SQL 文件**

`frontend/src/lib/db/migrations/0036_drop_v_running_timeboxes_recreate.sql` (new):

```sql
-- [026.02.3.1] T2 — TD-025 v_running_timeboxes view stale filter 修复
--
-- [023.12] (2026-07-06) timebox.status 6→3 态收敛: 删 'running'/'overtime'/'ended'
-- 改读时派生 (derive-display-status.ts)。原 v_running_timeboxes view WHERE status IN
-- ('running', 'overtime') 永远空集, 实际 production 已无人调用但 doc stale。
--
-- 修复: 派生「当前运行中」→ planned AND now ∈ [start_time, end_time]
-- 触发替代: 时间态 'overtime' (logged + 已过 end) 不在本 view; 由 application
-- derive-display-status.ts 派生 'overtime' badge。
--
-- DDL: DROP IF EXISTS + CREATE OR REPLACE (跨 PG 版本幂等)
DROP VIEW IF EXISTS public.v_running_timeboxes;

CREATE OR REPLACE VIEW public.v_running_timeboxes AS
SELECT id, user_id, title, status, start_time, end_time, tags
FROM public.timeboxes
WHERE status = 'planned'
  AND start_time <= NOW()
  AND end_time >= NOW();

COMMENT ON VIEW public.v_running_timeboxes IS
  '[026.02.3.1] TD-025 fix: derive running timeboxes as planned + now ∈ [start,end]';
```

- [ ] **Step 3: 登记 journal idx=36**

Find `frontend/src/lib/db/migrations/_journal.md` (or `drizzle-journal.md`, `migrations/_journal.txt`). Read head, append entry:

```md
| 36 | 0036_drop_v_running_timeboxes_recreate | [026.02.3.1] T2 | TD-025 view stale filter 修复 |
```

If file doesn't exist, README / progress ledger suffice for now — will verify in dev DB 跑通.

- [ ] **Step 4: 同步 USOM 时间态派生语义注释**

`docs/usom-design.md` find §3.9 timebox section header (likely around line 800-900 per memory), 在 "时间态 (running/overtime/in_progress/expired) 改读时派生" 段加注释:

```md
> [026.02.3.1] T2 对齐: DB view `v_running_timeboxes` 现派生 `status='planned' AND now ∈ [start_time, end_time]`，与本节「时间态派生显示」语义对齐。SQL 实现见 database-design.md 第十节 + migrations 0036。
```

- [ ] **Step 5: dev DB 迁移验证 (实际 SQL 跑通)**

Run: `cd frontend && npx tsx scripts/migrate-0036-dev.ts` 或 grep 实际 migrate 脚本名:
- 若不存在: cd frontend, 用 psql 直连: `PGPASSWORD=postgres psql -h localhost -p 5432 -U postgres -d lifeware_dev -f src/lib/db/migrations/0036_drop_v_running_timeboxes_recreate.sql`
- Expected: 
  - `DROP VIEW` (1 row affected)
  - `CREATE VIEW` (1 row affected)
  - `COMMENT` (1 row affected)
  - `\dv v_running_timeboxes` 应显示 view 存在

Run (实际验证 view 行为): 同 psql session:
```sql
-- insert 一个时间窗内的 planned timebox
INSERT INTO timeboxes (user_id, status, title, start_time, end_time, schema_version)
VALUES ('00000000-0000-0000-0000-000000000001', 'planned', 'test', NOW() - INTERVAL '5 minutes', NOW() + INTERVAL '5 minutes', 1);

SELECT count(*) FROM v_running_timeboxes; -- 应 >= 1

DELETE FROM timeboxes WHERE title = 'test';
```

- [ ] **Step 6: 验证迁移可重跑 (幂等)**

Run: 重复跑 Step 5 SQL
Expected: 0 errors (DROP IF EXISTS + CREATE OR REPLACE 幂等)

- [ ] **Step 7: Commit T2**

```bash
cd /home/walker/lifeware
git add docs/database-design.md \
        docs/usom-design.md \
        frontend/src/lib/db/migrations/0036_drop_v_running_timeboxes_recreate.sql
# journal 文件若改动:
# git add frontend/src/lib/db/migrations/_journal.md # 或 .json
git commit -m "fix(026.02.3.1): T2 v_running_timeboxes view stale filter 重写

[023.12] (2026-07-06) timebox.status 收敛 6→3 态后, 旧 view WHERE status IN
('running', 'overtime') 引用已删除 status, 永远空集。

重写派生: planned AND now ∈ [start_time, end_time] — 与 USOM 时间态派生
语义对齐 (derive-display-status.ts)。

- docs/database-design.md:1548-1557 SQL 块替换
- docs/usom-design.md §3.9 增派生注释
- migrations/0036 DROP IF EXISTS + CREATE OR REPLACE (跨 PG 版本幂等)
- dev DB 跑通 (psql lifeware_dev) — view count test 通过

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 3: TD-026 ai_sessions docs Markdown table → SQL block

**Files:**
- Modify: `docs/database-design.md:1703-1723`

**Interfaces:**
- Consumes: `frontend/src/lib/db/schema.ts:619-638` ai_sessions schema 定义
- Produces: docs §8.x `ai_sessions` 段改 ```sql CREATE TABLE``` block 格式, 与 §4.x `user_settings`/`memory_episodes` 同模式

- [ ] **Step 1: Read schema.ts ai_sessions 段落, capture 完整列**

Run: `cd frontend && sed -n '615,640p' src/lib/db/schema.ts`
Expected: 看到 `pgTable('ai_sessions', { id, user_id, schema_version, title, status, domain_id, action, session_mode, messages, state_snapshot, referenced_object_ids, created_at, updated_at, archived_at })`

(Implementer 实际执行时按 grep 输出补全 — 此处用 schema 实际形态 placeholder, implementer 复制粘贴即可)

- [ ] **Step 2: 替换 docs/database-design.md §8.1 `ai_sessions` 段**

Read `docs/database-design.md:1700-1730`. 现 Markdown table 段删除, 替换为:

```sql
CREATE TABLE ai_sessions (
  id                    uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id               uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  schema_version        integer NOT NULL DEFAULT 1,

  -- 基础字段
  title                 text NOT NULL DEFAULT '新对话',

  -- [026.02.3.1] T1 三向一致: status 6 值与 USOM `AISessionStatus` 完全一致
  -- (created/active/completing/archived/closed/deleted)
  status                text NOT NULL DEFAULT 'created'
                          CHECK (status IN ('created', 'active', 'completing', 'archived', 'closed', 'deleted')),

  -- Domain 路由字段 (l1/l2)
  domain_id             text,
  action                text,

  -- 会话模式 (single_shot default)
  session_mode          text NOT NULL DEFAULT 'single_shot',

  -- JSONB 内容: 消息流 + 状态快照 + 引用对象
  messages              jsonb NOT NULL DEFAULT '[]',  -- ChatMessage[]
  state_snapshot        jsonb NOT NULL DEFAULT '{}',  -- 状态快照
  referenced_object_ids jsonb NOT NULL DEFAULT '[]',  -- 引用对象 ID 列表

  -- 审计字段
  created_at            timestamptz NOT NULL DEFAULT NOW(),
  updated_at            timestamptz NOT NULL DEFAULT NOW(),
  archived_at           timestamptz
);

CREATE INDEX idx_ai_sessions_user_status ON ai_sessions(user_id, status);
CREATE INDEX idx_ai_sessions_updated     ON ai_sessions(user_id, updated_at);
```

- [ ] **Step 3: 验证 — §二 总览 vs §8.1 段 vs schema.ts 三向对齐**

Read line 170 — §二 总览 ascii tree (应仍有 `ai_sessions ← AI 会话` 一行,不改)
Read line 1669 — `表结构总览` 表 (应仍有 `ai_sessions | AI 会话表` 一行,不改)

Run: `cd frontend && grep -c "ai_sessions" docs/database-design.md`
Expected: ~10+ 引用 (§二, §8.1, §8.4 等)

Run: `cd frontend && grep "CREATE TABLE ai_sessions" docs/database-design.md`
Expected: 1 行 (本次新加)

- [ ] **Step 4: Commit T3**

```bash
cd /home/walker/lifeware
git add docs/database-design.md
git commit -m "docs(026.02.3.1): T3 ai_sessions §8.x Markdown table → SQL block 格式统一

[026.02.3.1] /lifeware-neat 重扫 TD-026: ai_sessions §8.x 用 Markdown table
展示, 与邻居 user_settings/memory_episodes 用 \`\`\`sql CREATE TABLE\`\`\`
block 格式不一致。

本任务统一格式, 与 §4.x 一致, 列与 schema.ts:619-638 完全对齐(含
schema_version + 6 值 status CHECK + 2 索引)。

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 4: TD-027 docs 页脚 07-07 → 07-09 + 4 变更段补

**Files:**
- Modify: `docs/usom-design.md` (末尾 footer + 4 变更段插入)
- Modify: `docs/database-design.md` (末尾 footer + 4 变更段插入)

**Interfaces:**
- Consumes: 现 footer `*文档版本：2026_07_07*` + 现变更段
- Produces: footer → `2026_07_09` + 补 [026.02]/[026.02.1]/[026.02.2]/[026.02.3] 4 段变更记录

- [ ] **Step 1: 确认 footer 当前状态**

Run:
```bash
tail -10 docs/usom-design.md
tail -10 docs/database-design.md
```
Expected: 两个 footer 都是 `2026_07_07`, 变更段保留到 `[023.13]` / `[026.01]` (其他历史已截断)

- [ ] **Step 2: Modify docs/usom-design.md footer**

Read tail 6 行 of `docs/usom-design.md`, replace:

```diff
-*文档版本：2026_07_07*
+*文档版本：2026_07_09*
 *关联上级文档：LW_overall_总体设计_2026_05_02.md*
 *关联数据库文档：docs/database-design.md*

-*变更：[023.13] (2026_07_07) — §3.9 DetailedExecutionRecord 扩展 4 字段 `actualStartTime?/actualEndTime?/focusMinutes?/energyActual?`（打卡专区；JSONB 演进免 DDL；energyActual 单值绕 D8 4 维禁令）*
-*变更：[023.11] (2026_07_06) — §3.11 ActivityArchetype 加 `synonyms: string[]` 字段（jsonb 列于 activity_archetypes；用于标题→archetype 匹配；空=未维护）*
-*变更：[023.05] PR2 阶段 2 (2026_07_05) — §3.13 Itinerary→Appointment 全层重命名 + 设计覆盖注（schedule→appointment 因 timebox 语义撞车）+ 中文「行程」→「约定」*
+*变更：[026.02.3.1] (2026_07_09) — T1 AISessionStatus 三向一致 (USOM 扩 6 值 + 删局部 SessionStatus 别名)；T2 v_running_timeboxes 派生语义重写（migration 0036）*
+*变更：[026.02.3] (2026_07_09) — /editAppointment selecting→编辑视图 Runtime TypeError 双层防御（handlers.ts todayAppointments 5→8 字段 + AppointmentFormFields ?? fallback + IRON RULE 测试）*
+*变更：[026.02.2] (2026_07_09) — [026.02.1] 登记的 7 项 polish 收口（M-4 ymdKey DRY + I-2 「条」后缀 + M-1 as any 移除 + M-2 mock rename + M-3 fake timers + M-6 Promise.allSettled + M-5 UX defer）*
+*变更：[026.02] (2026_07_08) — §1 [026.01] CNUI client 漏注册回归修复 + IRON RULE guardian；§2 /appointments Day/Month 双视图重构 + status/日期范围筛选 + 加载窗口 7d→90d*
+*变更：[026.02.1] (2026_07_08) — I-1 mk() TS2322 修复（commit 22ac0a7）+ 7 项 polish 登记（I-2 + M-1..M-6 + TD-022 5 项延后）*
+*变更：[023.13] (2026_07_07) — §3.9 DetailedExecutionRecord 扩展 4 字段 `actualStartTime?/actualEndTime?/focusMinutes?/energyActual?`（打卡专区；JSONB 演进免 DDL；energyActual 单值绕 D8 4 维禁令）*
+*变更：[023.11] (2026_07_06) — §3.11 ActivityArchetype 加 `synonyms: string[]` 字段（jsonb 列于 activity_archetypes；用于标题→archetype 匹配；空=未维护）*
+*变更：[023.05] PR2 阶段 2 (2026_07_05) — §3.13 Itinerary→Appointment 全层重命名 + 设计覆盖注（schedule→appointment 因 timebox 语义撞车）+ 中文「行程」→「约定」*
```

- [ ] **Step 3: Modify docs/database-design.md footer**

Same pattern, replace tail 5 行:

```diff
-*文档版本：2026_07_07*
+*文档版本：2026_07_09*
 *关联上游文档：docs/usom-design.md*

-*变更：[023.13] (2026_07_07) — §4.7 timeboxes.execution_record JSONB 形状扩展 4 可选字段（actualStartTime/actualEndTime/focusMinutes/energyActual），免 DDL 迁移（JSONB 演进）*
-*变更：[023.11] (2026_07_06) — §7.6 activity_archetypes 加 `synonyms jsonb NOT NULL DEFAULT '[]'` 列（迁移 0034_023_11_archetype_synonyms.sql，幂等 ADD COLUMN IF NOT EXISTS）*
-*变更：[026.01] (2026_07_07) — §appointments 加 `activity_archetype_id uuid REFERENCES activity_archetypes(id) ON DELETE SET NULL` 列 + 索引 `idx_appointments_archetype`，迁移 0035_026_01_appointment_activity_archetype.sql（IF NOT EXISTS 幂等）*
-*变更：[023.05] PR2 阶段 2 (2026_07_05) — §4.X appointments（rename 自 itineraries）+ 0033 RENAME 迁移 + F2 snapshot drift acknowledge（drizzle snapshot 停 0006，未来 schema 变更继续手写 SQL + 登记 journal，不引入 `drizzle-kit up`）*
+*变更：[026.02.3.1] (2026_07_09) — T1 AISessionStatus 三向一致（schema.ts ai_sessions CHECK 6 值与 USOM 对齐）+ T2 v_running_timeboxes 视图重写（迁移 0036 派生 planned AND now∈[start,end]）+ T3 §8.x ai_sessions 表格式 Markdown→SQL block 统一*
+*变更：[026.02.3] (2026_07_09) — /editAppointment selecting→编辑视图 Runtime TypeError 双层防御（handlers.ts:268-274 todayAppointments mapper 补 3 字段）*
+*变更：[026.02.2] (2026_07_09) — 7 项 polish 收口（架构影响限于 timebox/cnui/filter 等域，无 schema/db 变更）*
+*变更：[026.02] (2026_07_08) — §1 + §2（架构影响限于页面 UI 与 CNUI surface 注册，无 schema/db 变更）*
+*变更：[026.02.1] (2026_07_08) — I-1 mk() 类型签名修复（commit 22ac0a7，无 schema/db 变更）*
+*变更：[023.13] (2026_07_07) — §4.7 timeboxes.execution_record JSONB 形状扩展 4 可选字段（actualStartTime/actualEndTime/focusMinutes/energyActual），免 DDL 迁移（JSONB 演进）*
+*变更：[023.11] (2026_07_06) — §7.6 activity_archetypes 加 `synonyms jsonb NOT NULL DEFAULT '[]'` 列（迁移 0034_023_11_archetype_synonyms.sql，幂等 ADD COLUMN IF NOT EXISTS）*
+*变更：[026.01] (2026_07_07) — §appointments 加 `activity_archetype_id uuid REFERENCES activity_archetypes(id) ON DELETE SET NULL` 列 + 索引 `idx_appointments_archetype`，迁移 0035_026_01_appointment_activity_archetype.sql（IF NOT EXISTS 幂等）*
+*变更：[023.05] PR2 阶段 2 (2026_07_05) — §4.X appointments（rename 自 itineraries）+ 0033 RENAME 迁移 + F2 snapshot drift acknowledge（drizzle snapshot 停 0006，未来 schema 变更继续手写 SQL + 登记 journal，不引入 `drizzle-kit up`）*
```

- [ ] **Step 4: 验证两个 footer**

Run:
```bash
tail -10 docs/usom-design.md
tail -10 docs/database-design.md
```
Expected: 双方都是 `2026_07_09`, 8 变更段 (T 本任务 + 4 上一段 + 3 历史)

- [ ] **Step 5: Commit T4**

```bash
cd /home/walker/lifeware
git add docs/usom-design.md docs/database-design.md
git commit -m "docs(026.02.3.1): T4 docs 页脚 07-07→07-09 + 4 变更段补

TD-027: docs/usom-design.md + docs/database-design.md 末 footer 仍是
2026_07_07, 自此后 [026.02.X] 4 段 (07-08/07-09) 变更未登记。同步 bump
+ 补全 4 段变更 (含本任务 [026.02.3.1])。

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 5: 5 cosmetic minor polish from [026.02.2]

**Files (5 处独立修改):**
- Modify: `CHANGELOG.md` line 338 (C1 + C3)
- Modify: `docs/superpowers/specs/2026-07-09-026-02-2-appointment-polish-design.md` (C2)
- Modify: `docs/superpowers/plans/2026-07-09-026-02-2-appointment-polish.md` (C2)
- Modify: `frontend/src/domains/timebox/lib/__tests__/appointment-filter.test.ts` mk() comment (C4)
- Modify: `frontend/src/domains/timebox/components/__tests__/appointment-workspace.test.tsx:294` (C5)

**Interfaces:**
- Consumes: 现 5 处 stale 字面/注释
- Produces: 1:1 替换, 无 functional change

- [ ] **Step 1: C1 — CHANGELOG.md M-1 count 8 → 11**

Read `CHANGELOG.md:338` (modify line in M-1 entry):

原 (line 338 in [026.02.2] 改动清单 section):
```md
- **M-1**：`appointment-workspace.test.tsx` 移除 8 处 `as any`
```

替换:
```md
- **M-1**：`appointment-workspace.test.tsx` 移除 11 处 `as any`（brief 估 8 处, 实际代码扫描 11 处 — 全部用 proper USOM ID 类型替代；count drift 由 reviewer 抓, ship-then-polish 收口）
```

- [ ] **Step 2: C3 — CHANGELOG.md tsc 0 新增修订**

Modify the same section (verify result line):

原:
```md
- tsc 变更文件 0 新增错误
```

替换:
```md
- tsc 变更文件 0 新增语义错误（2 个新 TS2304 baseline noise，vitest-globals 项目 pre-existing 模式）
```

- [ ] **Step 3: C2 — spec/plan I-2 "4 处 test" → 2 处**

Read `docs/superpowers/specs/2026-07-09-026-02-2-appointment-polish-design.md`. Search for "4 处 test" or "4 test contract"：

Replace 一处: 把 "4 test contract" → "2 test contract" + 加注:
```md
- **I-2**：`appointment-month-view.tsx:88` 加「条」后缀 + 2 test contract 调整
  (brief 估 4 处, 实际合并到 1 个 `it()` 块下, 2 个 test contract — count drift 由 reviewer 抓, ship-then-polish 收口)
```

Read `docs/superpowers/plans/2026-07-09-026-02-2-appointment-polish.md`. Search for "4 处 test" or "4 test contract"：

Replace (相同字面, 类似注):
```md
- [ ] **Step X: 测试 + 验证** — 2 个 test contract 调整 (brief 估 4 处, 实际合并; count drift 由 reviewer 抓)
```

- [ ] **Step 4: C4 — appointment-filter.test.ts mk() comment 修复**

Read `frontend/src/domains/timebox/lib/__tests__/appointment-filter.test.ts`. Find mk() 函数 + 「本月惯例」注释:

替换:
```ts
// 月视图格子计数「条」后缀补回 — fix UI 约定
// 本月惯例改为「本月约定」(filter test fixture) — D1=A
const mk = (overrides: Partial<TestItem> = {}): TestItem => ({
  // ...
})
```

(Note: 实际原文「本月惯例」字眼在 deliverable 5cf3b73 commit message 改 review minor 时标注; implementer 按 grep 实际匹配替换)

- [ ] **Step 5: C5 — appointment-workspace.test.tsx mock shape 修复**

Read `frontend/src/domains/timebox/components/__tests__/appointment-workspace.test.tsx:294`. 现:

```ts
vi.mocked(deleteAppointment)
  .mockResolvedValueOnce({ ok: true } as any)
  .mockRejectedValueOnce(new Error('boom'))
```

替换:
```ts
vi.mocked(deleteAppointment)
  .mockResolvedValueOnce({ status: 'ok', appointment: { id: 'a-1' } })
  .mockRejectedValueOnce(new Error('boom'))
```

(对齐 default mock shape `deleteAppointment: vi.fn().mockResolvedValue({ status: 'ok', appointment: { id: 'it-1' } })` 在 line 37)

- [ ] **Step 6: 验证 (vitest pass; tsc 0 新增)**

Run: `cd frontend && npx vitest run src/domains/timebox/components/__tests__/appointment-workspace.test.tsx src/domains/timebox/lib/__tests__/appointment-filter.test.ts`
Expected: 16 + N tests pass, baseline 0 回归 (任 mock shape 调整不应触发 27 个测试 fail)

Run: `cd frontend && npx tsc --noEmit 2>&1 | grep -E "appointment-workspace.test|appointment-filter.test" | wc -l`
Expected: 与 baseline (=15) 持平 (无新错)

- [ ] **Step 7: Commit T5 (1 atomic commit, 5 文件改动)**

```bash
cd /home/walker/lifeware
git add CHANGELOG.md \
        docs/superpowers/specs/2026-07-09-026-02-2-appointment-polish-design.md \
        docs/superpowers/plans/2026-07-09-026-02-2-appointment-polish.md \
        frontend/src/domains/timebox/lib/__tests__/appointment-filter.test.ts \
        frontend/src/domains/timebox/components/__tests__/appointment-workspace.test.tsx
git commit -m "polish(026.02.3.1): T5 5 cosmetic minor ship-then-polish 收口

[026.02.2] whole-branch review 5 cosmetic minor 收口 (reviewer minor, ship-then-polish 可接受):

- C1: CHANGELOG.md M-1 count \"8 处\" → \"11 处\" (实际扫描) + 注释
- C2: spec/plan I-2 \"4 处 test\" → \"2 处 test contract\" + 注释
- C3: CHANGELOG \"tsc 0 新增\" → \"tsc 0 新增语义错误 (2 TS2304 baseline)\"
- C4: appointment-filter.test.ts mk() \"本月惯例\" → \"本月约定筛选 (fixture)\"
- C5: appointment-workspace.test.tsx:294 mock \`{ ok: true } as any\` →
       \`{ status: 'ok', appointment: { id: 'a-1' } }\` 对齐 default shape

5 处 1:1 替换, 无 functional change。

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 6: CHANGELOG `[026.02.3.1]` 段 + manifest 入口

**Files:**
- Modify: `CHANGELOG.md` (insert new `## [026.02.3.1]` 段 in 1.x index mode)
- Modify: `manifest.md` (add `# [026.02.3.1]` entry)

**Interfaces:**
- Consumes: 现 CHANGELOG 结构 (`[026.02.3]` 在 line 367-414 位置)
- Produces: 新段落在 [026.02.3] 段后, manifest 在 [026.02.3] 段后

- [ ] **Step 1: 插入 CHANGELOG `[026.02.3.1]` 段**

Read `CHANGELOG.md:416` (after [026.02.3] segment 末尾 `---` 之后).

Insert new segment 在 [026.02.3] 段与「项目宪章」section 之间:

````markdown
---

## [026.02.3.1] 4 项 fresh drift 修复 + 5 cosmetic minor 收口（2026-07-09）

> [026.02.3] ship 后 `/lifeware-neat` 5 数据源重扫发现的 4 项 fresh drift 全部 ship。Items 1+2（TaskStatus/HabitStatus USOM drift）由前会话扫描时报 drift 但实际 main 已对齐，验证后从 scope 删除。
> 同 PR 收 [026.02.2] whole-branch review 5 cosmetic minor polish。

### 决策摘要

- **4 项 drift 全部 ship**：TD-024 (AISessionStatus 三向一致) + TD-025 (v_running_timeboxes view stale filter) + TD-026 (ai_sessions docs 格式) + TD-027 (docs 页脚过期)
- **5 cosmetic minor 收口**：[026.02.2] whole-branch review 5 项 polish 全部 ship
- **scope 不动**：type cast 透明性 (`EditAppointment.tsx:32`) 仍留 [026.02.4] 范围（与 TD-022 5 项同）

### 改动清单

- **T1 TD-024**：`primitives.ts:230` `AISessionStatus` 扩 3 值 (`created`/`completing`/`closed`) + 删 `session/index.ts` 局部 `SessionStatus` 别名 + 新增 `session-status.test.ts` IRON RULE (6 值 type-level + runtime array 双校验)
- **T2 TD-025**：`docs/database-design.md:1548-1557` view SQL 重写 (`status='planned' AND start_time<=NOW() AND end_time>=NOW()`) + `migrations/0036_drop_v_running_timeboxes_recreate.sql` 新建（journal idx=36，跨 PG 幂等 DROP IF EXISTS + CREATE OR REPLACE）+ dev DB 跑通验证
- **T3 TD-026**：`docs/database-design.md:1703-1723` §8.x ai_sessions Markdown table → ```sql CREATE TABLE``` block 格式统一（与 §4.x user_settings/memory_episodes 同模板）
- **T4 TD-027**：`docs/usom-design.md` + `docs/database-design.md` 页脚 `2026_07_07 → 2026_07_09` bump + 补 [026.02]/[026.02.1]/[026.02.2]/[026.02.3] 4 段变更记录
- **T5 5 cosmetic minor**：
  - C1: CHANGELOG M-1 count "8 处" → "11 处" + 注释
  - C2: spec/plan I-2 "4 处 test" → "2 处 test contract" + 注释
  - C3: CHANGELOG tsc "0 新增" → "0 新增语义错误 (2 TS2304 baseline)" + 注释
  - C4: `appointment-filter.test.ts` mk() comment "本月惯例" → "本月约定筛选 (fixture)"
  - C5: `appointment-workspace.test.tsx:294` `{ ok: true } as any` → `{ status: 'ok', appointment: { id: 'a-1' } }` 对齐 default mock shape

### 验证结果

- vitest: 0 回归（baseline=head）
- tsc: 0 新增错误
- pre-push hooks: `validate:manifest` 0 errors + `validate:structure` ✓ + `validate:rules-registry` 6 项一致
- USOM ↔ DB 双向互验 2A-2F 重扫: 0 新增 drift
- IRON RULE: `session-status.test.ts` 通过（6 值 shape 锁定，TypeScript + runtime 双校验）

### 风险与缓解

- **T1 类型 cast 透明性**：`EditAppointment.tsx:32` `as (AppointmentDraftFields & { status: string })[]` 仍遮蔽 runtime 形状，**未触及**（属 [026.02.4] / TD-022 同范围）；T1 仅扩 USOM 6 值对齐 DB + code，未改 surface 类型 cast
- **T2 view 替代品风险**：`v_running_timeboxes` 替代 SQL 与 `derive-display-status.ts` 派生语义等价，但 production 实际调用方需 grep 确认（已确认 production 无查询代码，仍是 documentation/audit 用途）
- **T3 docs 格式单点**：ai_sessions 一处改动，列与 schema.ts 已逐字对齐，无歧义

### 关联

- 上游：[026.02.3] commit e97b9a4 + 3ffc2c9 + c220e15（`/editAppointment TypeError 双层防御` 已 ship）
- 重扫：[`/lifeware-neat` 5 数据源] (2026-07-09) — 发现 4 项 fresh drift
- Spec SSOT: `docs/superpowers/specs/2026-07-09-026-02-3-1-follow-up-fixes-design.md` (commit 17db764)
- Plan SSOT: `docs/superpowers/plans/2026-07-09-026-02-3-1-follow-up-fixes.md`

### 后续 defer（不在本轮）

- TD-022 5 项（archetype clearing / UUID 验证 / perf N+1 / originalPrompt banner / 浏览器 E2E）→ [026.02.4]
- `EditAppointment.tsx:32` 类型 cast 透明性 → [026.02.4]

---

````

(整个段插入)

- [ ] **Step 2: 验证 CHANGELOG 段位置**

Run: `cd /home/walker/lifeware && grep -n "^## \[026.02" CHANGELOG.md`
Expected: `[026.02]` `[026.02.1]` `[026.02.2]` `[026.02.3]` `[026.02.3.1]` (新增在最末)

- [ ] **Step 3: manifest.md 加 [026.02.3.1] 入口**

Read `manifest.md:54-56` (after [026.02.3] line).

Insert 一行新 entry (after [026.02.3] line, before [023.05-2]):

```md
# [026.02.3.1] 4 项 fresh drift 修复 + 5 cosmetic minor 收口（2026-07-09）— 1 PR 6 task ship-ready（commit TBD after T1-T6 ship）；来自 [026.02.3] ship 后 `/lifeware-neat` 5 数据源重扫。覆盖：TD-024 `AISessionStatus` USOM 3→6 值三向一致（USOM + DB schema + 实际代码 transite map）/ TD-025 `v_running_timeboxes` 视图 stale filter 重写（派生 `status='planned' AND now∈[start,end]`，迁移 0036）/ TD-026 `ai_sessions` §8.x 文档格式 Markdown table → SQL block 统一 / TD-027 `docs/usom-design.md` + `docs/database-design.md` 页脚 `2026_07_07 → 2026_07_09` + 4 变更段补 + [026.02.2] whole-branch review 5 cosmetic minor polish 收口（C1/C2/C3/C4/C5 数字注释对齐）。Authority：spec SSOT (`docs/superpowers/specs/2026-07-09-026-02-3-1-follow-up-fixes-design.md`, commit 17db764) + plan SSOT (本文档并行 commit) + CHANGELOG.md `## [026.02.3.1]`。Whole-branch verification：tsc 0 新增 / vitest baseline=head 0 回归 / pre-push hooks 全过 / `validate:manifest` 0 errors / `validate:structure` ✓ / `validate:rules-registry` 6 项一致 / IRON RULE `session-status.test.ts` pass。延后：TD-022 5 项（archetype clearing / UUID 验证 / newDurationMin 语义 / perf N+1 / originalPrompt banner）+ EditAppointment.tsx:32 类型 cast 透明性 → [026.02.4]。
```

- [ ] **Step 4: 验证 manifest 索引**

Run: `grep -n "^# \[026" manifest.md`
Expected: `[026]`、`[026.02]`、`[026.02.1]`、`[026.02.2]`、`[026.02.3]`、`[026.02.3.1]` (新增在最末)

- [ ] **Step 5: 跑全套 pre-push hooks (Ship 前 final 验证)**

Run:
```bash
cd /home/walker/lifeware/frontend
npm run validate:manifest 2>&1 | tail -3
npm run validate:structure 2>&1 | tail -3
npm run validate:rules-registry 2>&1 | tail -3
```
Expected: 0 errors ✓ / 全部通过 ✓ / 6 项一致 ✓

Run: `cd frontend && npx tsc --noEmit 2>&1 | wc -l`
Expected: = 95 (baseline, 0 新增)

Run: `cd frontend && npx vitest run src/nexus/ai-runtime src/domains/timebox/components/__tests__ src/domains/timebox/lib/__tests__ 2>&1 | tail -5`
Expected: baseline 失败集合 0 新增 + IRON RULE pass

- [ ] **Step 6: Commit T6 (1 atomic, 2 files)**

```bash
cd /home/walker/lifeware
git add CHANGELOG.md manifest.md
git commit -m "docs(026.02.3.1): T6 CHANGELOG + manifest 段

[026.02.3.1] 6 task ship-ready 总收口:
- T1-T5 全部 commit 上 main (commit hash by git log)
- CHANGELOG \`## [026.02.3.1]\` 段 (含决策/改动/验证/风险/关联/后续 defer)
- manifest.md \`# [026.02.3.1]\` 入口对齐

Authority: spec SSOT (commit 17db764) + plan SSOT (本 commit 平行) + 
上 5 task commits。

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 7: SDD Ship (Final Whole-Branch Review + Push)

(SDD final, 由 `superpowers:finishing-a-development-branch` skill 接管)

- [ ] **Step 1: 全 whole-branch review (代码 review 维度)**

Run 派遣 `code-reviewer` subagent:
- Diff range: `git diff main~6 main` (6 task commits)
- 关注: 5 drift 修复质量 + 5 cosmetic minor 精确性 + IRON RULE 测试覆盖
- 期望: 0 critical / ≤ 2 important (mostly mock shape 偏差) / cosmetic

- [ ] **Step 2: 处理 review findings**

如有 fixups, 派 fix subagent + 重审。

- [ ] **Step 3: `/record-tech-debt`** — TD-024..TD-027 4 instance + README 更新 (status 🟢 closed after T1-T4 ship)

- [ ] **Step 4: ff-merge main + worktree cleanup + push**

(`superpowers:finishing-a-development-branch` skill)
- Verify: tests pass + manifests updated
- Merge: ff into main (当前在 feat/026-02-3-1-follow-up-fixes branch)
- Cleanup: `git worktree remove`
- Push: `git push origin main`
- Pre-push hooks: validate:manifest + validate:structure + validate:rules-registry 全过

- [ ] **Step 5: 最终汇报**

## Overall Verification

| 验证项 | 命令 | 期望 |
|---|---|---|
| TypeScript 编译 | `cd frontend && npx tsc --noEmit 2>&1 \| wc -l` | = 95 行 baseline (0 新增) |
| vitest (timebox + ai-runtime) | `cd frontend && npx vitest run src/nexus/ai-runtime src/domains/timebox/components/__tests__ src/domains/timebox/lib/__tests__ 2>&1 \| tail -5` | baseline 失败集合 0 新增 |
| pre-push hooks | `cd frontend && npm run validate:manifest && npm run validate:structure && npm run validate:rules-registry` | 0 errors ✓ |
| IRON RULE: AISessionStatus 6 值 | `cd frontend && npx vitest run src/nexus/ai-runtime/__tests__/session-status.test.ts` | pass |
| IRON RULE: view 重写 | `cd frontend && PGPASSWORD=postgres psql -h localhost -p 5432 -U postgres -d lifeware_dev -f src/lib/db/migrations/0036_drop_v_running_timeboxes_recreate.sql` | 0 errors |
| USOM ↔ DB 互验 2A | `cd frontend && grep "^export type AISessionStatus" src/usom/types/primitives.ts && grep "ai_sessions.*enum" src/lib/db/schema.ts` | 6 值 = 6 值 |

## Self-Review (Plan Coverage)

1. **Spec coverage**：
   - TD-024 AISessionStatus 三向一致 → Task 1 ✅
   - TD-025 v_running_timeboxes view 重写 → Task 2 ✅
   - TD-026 ai_sessions docs 格式 → Task 3 ✅
   - TD-027 docs 页脚 → Task 4 ✅
   - 5 cosmetic minor polish → Task 5 ✅
   - CHANGELOG + manifest 段 → Task 6 ✅
   - SDD ship → Task 7 ✅
   - Total tasks = 6 + Ship task = 7 = 与 design spec 完全一致

2. **Placeholder scan**：
   - "TBD" 检查: Step 6 commit messages 留 TBD commit hash → Ship task 6 后补 fill
   - "TODO" 检查: 仅出现在实际代码注释 (mk() 改 "本月惯例" → "本月约定"),无 plan 内部 placeholder
   - "implement later" / "fill in details" / "Add appropriate error handling" / "Similar to Task N" — 无

3. **Type consistency**：
   - T1 `AISessionStatus` 6 值一致 (primitives.ts = schema.ts = database-design.md = session/index.ts transition map)
   - T5 mock shape `{ status: 'ok', appointment: { id: 'a-1' } }` 与 default mock line 37 一致
   - T6 manifest entry reference plan/spec SSOT commit hash 一致 (17db764)

## Out of Scope (Deferred to [026.02.4])

- TD-022 5 项 (archetype clearing / UUID 验证 / newDurationMin 语义 / perf N+1 / originalPrompt banner)
- `EditAppointment.tsx:32` 类型 cast 透明性
- `deleteAISession` server action (USOM `deleted` 字段预留, 无 server action 实现)
- v_running_timeboxes 加 overtime 派生 case when (server derive-display-status.ts 已覆盖, view 替代单点足够)

## SDD Workflow Summary

```
T1 + T2 + T3 + T4 + T5 + T6 (6 commits)
   ↓
Task 7: Whole-branch review
   ↓
T1-T5 fixups (if any)
   ↓
ff-merge main + worktree cleanup + push gitee
```

预计 SDD 总耗时 ~4-6 小时:
- T1: ~30 min (USOM 扩 + session test)
- T2: ~1 小时 (dev DB migrate 实操 + journal)
- T3: ~30 min (docs 一致性)
- T4: ~15 min (footer 替换)
- T5: ~30 min (5 处 1:1 替换)
- T6: ~30 min (CHANGELOG + manifest entry)
- T7 ship: ~1.5 小时 (whole-branch review + fixups + ff-merge + push + tech-debt 入库)

## 关联

- Spec: `docs/superpowers/specs/2026-07-09-026-02-3-1-follow-up-fixes-design.md` (commit 17db764)
- Memory: `~/.claude/projects/-home-walker-lifeware/memory/feedback_tier2-sync.md`
- 项目宪章: v2.1.1
- 上游 ship: e97b9a4 (fix) + 3ffc2c9 (CHANGELOG) + c220e15 (manifest) — `[026.02.3]`

## SSOT

- **本 plan** = `/home/walker/lifeware/docs/superpowers/plans/2026-07-09-026-02-3-1-follow-up-fixes.md`
- Spec: 上文链接 (commit 17db764)
- 落实 commits in `.superpowers/sdd/progress.md` (SDD 进行中由 implementer 更新)
