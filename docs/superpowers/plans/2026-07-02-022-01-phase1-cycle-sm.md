# [022.01] Phase 1: Cycle 写入口正规化 + 审核通过 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 Cycle 创建路径从直写 repo.save 改为经 executeIntent → SM → adapter.cycle.create 的正规路径，退役 [024.2] 临时修复，新增 manifest draft→in_progress 转换与"审核通过"UI。

**Architecture:** Phase 1 聚焦 Cycle 一级对象的写入正规化。adapter.cycle.create 实现后，executeIntent("createCycle") 走完整 Nexus 链（Intent Engine → Rule Engine → SM → adapter → repo.save）。审核通过按 server 时刻分派 plan(start) 或 start 两个 SM action。

**Tech Stack:** TypeScript 5, Next.js 16, Drizzle ORM, Vitest, React 19, Tailwind CSS 4

**Spec:** `docs/superpowers/specs/2026-07-02-022-01-okr-cycle-governance-design.md`

## Global Constraints

- 所有 Cycle 写入必须走 executeIntent 或 mutation-service.execute（宪法 §IX.1）
- Cycle 创建强制 draft 状态（不接受外部 status 入参）
- 自然键幂等：同 (userId, periodStart, periodEnd) 的 cycle 只存在一条
- onConflictDoUpdate 的 SET 不得覆写已有行的 status 字段
- 审核通过按 `now >= periodStart ? start : plan` 分派
- write-entry-guard 删除 createCycle 例外，扩展 `.insert(s.cycles)` 拦截

---

## 文件结构

| 文件 | 角色 | 操作 |
|------|------|------|
| `frontend/src/domains/okrs/repository/generic-repo-adapter.ts` | adapter.cycle.create / updateStatus 实现 | 修改 |
| `frontend/src/domains/okrs/repository/cycle.ts` | CycleRepository（save onConflictDoUpdate 改造 + findByPeriod） | 修改 |
| `frontend/src/domains/okrs/wiring.ts` | createOKROrchestrator + makeIntent 工厂（eng-review D1） | 新建 |
| `frontend/src/app/actions/okr.ts` | createCycle 改走 executeIntent；approveCycle 新增 | 修改 |
| `frontend/src/app/actions/okr-import.ts` | saveImportedOKRs 退役 [024.2]；导入 wiring.ts | 修改 |
| `frontend/src/domains/okrs/__tests__/write-entry-guard.test.ts` | 守卫：删例外 + 扩 .insert(s.cycles) | 修改 |
| `frontend/src/domains/okrs/manifest.yaml` | 新增 draft→in_progress 转换 + trigger 类型标注 | 修改 |
| `frontend/src/domains/okrs/components/cycle-create-drawer.tsx` | 去 in_progress 硬编码 + draft 文案 | 修改 |
| `frontend/src/domains/okrs/components/cycle-menu.tsx` | 新增（审核通过菜单项） | 新建 |
| `frontend/src/domains/okrs/components/__tests__/cycle-menu.test.tsx` | 审核通过菜单测试 | 新建 |
| `frontend/src/domains/okrs/repository/__tests__/cycle.test.ts` | adapter.create 幂等/降级防护测试 | 新建 |
| `frontend/src/app/actions/__tests__/okr-cycle.test.ts` | executeIntent createCycle 集成测试 | 新建 |

---

### Task 1: 实现 adapter.cycle.create + save SET 子句修复（合并，eng-review D2）

**Files:**
- Modify: `frontend/src/domains/okrs/repository/generic-repo-adapter.ts:153-155`
- Modify: `frontend/src/domains/okrs/repository/cycle.ts:41-62`（save onConflictDoUpdate SET 排除 status）
- Modify: `frontend/src/domains/okrs/repository/cycle.ts`（新增 findByPeriod）
- Create: `frontend/src/domains/okrs/repository/__tests__/cycle.test.ts`

**Interfaces:**
- Consumes: `repos.cycleRepo.findByPeriod`（新增）、`repos.cycleRepo.save`、Cycle USOM 类型
- Produces: `GenericRepo['cycle']['create']`；save onConflictDoUpdate 不再覆写 status

- [ ] **Step 1: 写 adapter.cycle.create 的失败测试**

```typescript
// frontend/src/domains/okrs/repository/__tests__/cycle.test.ts
/**
 * @file cycle.test
 * @brief [022.01] Phase 1: adapter.cycle.create 幂等与降级防护测试
 */
import { describe, it, expect, vi, beforeEach } from 'vitest'

// 因为 adapter.cycle.create 当前抛错，先测「不再抛错」
describe('[022.01] adapter.cycle.create', () => {
  it('不应再抛 "不支持通过 GenericRepo 创建" 错误', async () => {
    const { createOkrsGenericRepo } = await import('@/domains/okrs/repository/generic-repo-adapter')
    const repos = createOkrsGenericRepo({
      objectiveRepo: mockObjectiveRepo(),
      keyResultRepo: mockKeyResultRepo(),
      cycleRepo: mockCycleRepo(),
    })
    // adapter.cycle.create 被调用时应不再 throw
    await expect(
      repos.cycle.create({ cycleType: 'quarterly', name: '2026 Q3', periodStart: '2026-07-01', periodEnd: '2026-09-30' }, MVP_USER_ID)
    ).resolves.toBeDefined()
  })
})
```

- [ ] **Step 2: 运行测试验证失败**

```bash
cd frontend && npx vitest run src/domains/okrs/repository/__tests__/cycle.test.ts 2>&1 | tail -5
```
Expected: FAIL — `Cycle 不支持通过 GenericRepo 创建`

- [ ] **Step 3: 实现 adapter.cycle.create**

```typescript
// frontend/src/domains/okrs/repository/generic-repo-adapter.ts
// 替换 L153-155 的 cycle.create

async create(fields, userId, tx) {
  const now = new Date().toISOString() as Timestamp
  const periodStart = fields.periodStart as string
  const periodEnd = fields.periodEnd as string

  // ⚠️ iter 3: 前置 SELECT 查重 —— 防止 onConflictDoUpdate 覆写已有 cycle 的 status
  // 同自然键已存在 → 直接返回已有行（不写 DB），保护已有 cycle 不被降级为 draft
  const existing = await repos.cycleRepo.findByPeriod(
    userId, periodStart, periodEnd, tx,
  )
  if (existing) return existing

  const cycle: Cycle = {
    id: crypto.randomUUID() as USOM_ID,
    cycleType: (fields.cycleType as Cycle['cycleType']) ?? 'custom',
    name: (fields.name as string) ?? `${periodStart}~${periodEnd}`,
    period: { start: periodStart as DateOnly, end: periodEnd as DateOnly },
    status: 'draft', // manifest initial_state，强制 draft
    createdAt: now,
    updatedAt: now,
  }
  return repos.cycleRepo.save(cycle, userId, tx)
},
```

- [ ] **Step 4: 在 CycleRepository 新增 findByPeriod 方法**

```typescript
// frontend/src/domains/okrs/repository/cycle.ts
// 在 findByUserAndStatus 之后新增

/**
 * 按自然键 (userId, periodStart, periodEnd) 查找 cycle。
 * 供 adapter.cycle.create 前置查重使用，避免 onConflictDoUpdate 覆写已有 status。
 */
async findByPeriod(
  userId: USOM_ID,
  periodStart: string,
  periodEnd: string,
  tx: DbClient = db,
): Promise<Cycle | null> {
  const rows = await tx.select().from(s.cycles)
    .where(and(
      eq(s.cycles.userId, userId),
      eq(s.cycles.periodStart, periodStart),
      eq(s.cycles.periodEnd, periodEnd),
    ))
    .limit(1)
  return rows[0] ? cycleRowToUSOM(rows[0] as any) : null
}
```

- [ ] **Step 5: 运行测试验证通过**

```bash
cd frontend && npx vitest run src/domains/okrs/repository/__tests__/cycle.test.ts 2>&1 | tail -5
```
Expected: PASS

- [ ] **Step 6: 补充降级防护测试 —— 已有 in_progress cycle 不被覆写**

```typescript
it('同自然键已有 in_progress cycle 时，create 不覆写其 status（返回已有行）', async () => {
  const existingCycle = { id: 'existing-id', status: 'in_progress', /* ... */ }
  const mockCycleRepo = {
    findByPeriod: vi.fn().mockResolvedValue(existingCycle),
    save: vi.fn(),
  }
  const { createOkrsGenericRepo } = await import('@/domains/okrs/repository/generic-repo-adapter')
  const repos = createOkrsGenericRepo({
    objectiveRepo: mockObjectiveRepo(),
    keyResultRepo: mockKeyResultRepo(),
    cycleRepo: mockCycleRepo,
  })
  const result = await repos.cycle.create(
    { cycleType: 'quarterly', periodStart: '2026-07-01', periodEnd: '2026-09-30' },
    MVP_USER_ID,
  )
  expect(result).toBe(existingCycle)
  expect(mockCycleRepo.save).not.toHaveBeenCalled() // 未写入
})

it('自然键不存在时，create 构造 draft cycle 并 save', async () => {
  const mockCycleRepo = {
    findByPeriod: vi.fn().mockResolvedValue(null),
    save: vi.fn().mockResolvedValue({ id: 'new-id', status: 'draft' }),
  }
  const { createOkrsGenericRepo } = await import('@/domains/okrs/repository/generic-repo-adapter')
  const repos = createOkrsGenericRepo({
    objectiveRepo: mockObjectiveRepo(),
    keyResultRepo: mockKeyResultRepo(),
    cycleRepo: mockCycleRepo,
  })
  const result = await repos.cycle.create(
    { cycleType: 'quarterly', periodStart: '2026-07-01', periodEnd: '2026-09-30' },
    MVP_USER_ID,
  )
  expect(mockCycleRepo.save).toHaveBeenCalledTimes(1)
  expect(result).toEqual({ id: 'new-id', status: 'draft' })
})
```

- [ ] **Step 7: 运行全部测试**

```bash
cd frontend && npx vitest run src/domains/okrs/repository/__tests__/cycle.test.ts 2>&1 | tail -10
```
Expected: 3 tests PASS

- [ ] **Step 8: Commit**

```bash
git add frontend/src/domains/okrs/repository/generic-repo-adapter.ts frontend/src/domains/okrs/repository/cycle.ts frontend/src/domains/okrs/repository/__tests__/cycle.test.ts
git commit -m "feat(okrs): [022.01] adapter.cycle.create 实现 + 前置 SELECT 防降级

- adapter.cycle.create: 构造 draft Cycle → repo.save（不再抛错）
- CycleRepository.findByPeriod: 按自然键查重（adapter 前置闸）
- 同自然键已存在 → 返回已有行（不写 DB，防 onConflictDoUpdate 覆写 status）
- 测试：抛错消除 / 已有 in_progress 不降级 / 新建 draft"
```

---

### Task 2: createCycle 改走 executeIntent + wiring.ts 提取（eng-review D1）

**Files:**
- Create: `frontend/src/domains/okrs/wiring.ts`（提取 createOKROrchestrator + makeIntent）
- Modify: `frontend/src/app/actions/okr.ts:113-131,172,216`（createCycle 改走 executeIntent；wiring 函数导出移除，改为 import）
- Modify: `frontend/src/app/actions/__tests__/okr-cycle.test.ts`（eng-review D3 集成测试）

**Interfaces:**
- Consumes: `createOKROrchestrator`, `makeIntent`, `executeIntent`
- Produces: `createCycle(input)` 返回 `OKRActionResult<Cycle>`，内部走 SM create→draft

- [ ] **Step 1: 写 createCycle 走 executeIntent 的集成测试**

```typescript
// frontend/src/app/actions/__tests__/okr-cycle.test.ts
/**
 * @file okr-cycle.test
 * @brief [022.01] Phase 1: createCycle 走 executeIntent 集成测试
 */
import { describe, it, expect, vi } from 'vitest'

describe('[022.01] createCycle 走 executeIntent', () => {
  it('createCycle 应调用 orchestrator.executeIntent 而非 repo.save 直写', async () => {
    // Mock orchestrator 验证调用链
    const mockExecuteIntent = vi.fn().mockResolvedValue({ success: true })
    vi.mock('@/nexus/orchestrator', () => ({
      createOrchestrator: vi.fn(() => ({
        executeIntent: mockExecuteIntent,
      })),
    }))

    const { createCycle } = await import('@/app/actions/okr')
    // 注：此测试需要完整的 mock 链，实际需按项目现有的 orchestrator mock 模式调整
  })

  it('createCycle 不接受 status 入参（强制 draft）', async () => {
    // 验证 createCycle 的类型签名不含 status 参数
    // 实际通过检查 makeIntent 调用中的 fields 不含 status 来验证
  })
})
```

- [ ] **Step 2: 改造 createCycle 函数**

```typescript
// frontend/src/app/actions/okr.ts
// 替换 createCycle（原 L113-131）

/**
 * 创建周期（[022.01] Phase 1：改走 executeIntent → SM create→draft）
 *
 * 不再接受 status 入参——SM 按 manifest create→draft 强制 draft。
 * 自然键幂等：同 (userId, periodStart, periodEnd) 只存在一条 cycle。
 *
 * @param input - 周期输入数据
 * @returns 创建结果
 */
export async function createCycle(
  input: { cycleType: string; name: string; periodStart: string; periodEnd: string },
): Promise<OKRActionResult<Cycle>> {
  try {
    const orchestrator = await createOKROrchestrator()
    const intent = makeIntent('createCycle', {
      cycleType: input.cycleType,
      name: input.name,
      periodStart: input.periodStart,
      periodEnd: input.periodEnd,
    })
    const result = await orchestrator.executeIntent(intent, MVP_USER_ID)
    if (!result.success) return { success: false, error: result.error }

    // 从 SM 执行结果取返回对象（adapter.create → save 回查返回持久化行）
    const cycle = result.object as Cycle | undefined
    if (!cycle) return { success: false, error: '周期创建成功但未返回对象' }
    return { success: true, data: cycle }
  } catch (err) {
    return { success: false, error: err instanceof Error ? err.message : '创建周期失败' }
  }
}
```

- [ ] **Step 3: 删除旧的 createCycle 直写注释**

删除原 L114-122 的「走 repo.save 直写是有意为之的例外…」注释块。

- [ ] **Step 4: 验证 tsc 无新增错误**

```bash
cd frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"
```
Expected: 不新增 TS 错误（baseline 60 不变）

- [ ] **Step 5: Commit**

```bash
git add frontend/src/app/actions/okr.ts
git commit -m "feat(okrs): [022.01] createCycle 改走 executeIntent → SM create→draft

- createCycle 不再直调 repo.save，改走 orchestrator.executeIntent('createCycle')
- 移除 status 入参（SM 强制 draft）
- 删除旧的「有意为之的例外」注释"
```

---

### Task 3: findOrCreateCycle 改走 executeIntent + 退役 [024.2]

**Files:**
- Modify: `frontend/src/app/actions/okr-import.ts:165-178`（saveImportedOKRs 中的 findOrCreateCycle 调用）

**Interfaces:**
- Consumes: `createOKROrchestrator` (import from okr.ts)、`makeIntent`、`executeIntent`
- Produces: 导入创建的 cycle 走 SM create→draft；删除 `status: 'in_progress'` 硬编码

- [ ] **Step 1: 修改 saveImportedOKRs 中的 cycle 创建逻辑**

```typescript
// frontend/src/app/actions/okr-import.ts
// 在 saveImportedOKRs 函数中，替换 L165-178 的 cycle 创建部分

// 旧代码：
// const cycle = await cycleRepo.findOrCreateCycle(
//   MVP_USER_ID, periodType, okr.periodStart!, okr.periodEnd!, 'in_progress',
// )

// 新代码：
import { createOKROrchestrator } from './okr' // 需导出 createOKROrchestrator

// ... 在 saveImportedOKRs 内部：
const orchestrator = await createOKROrchestrator()
const cycleIntent = makeIntent('createCycle', {
  cycleType: periodType,
  name: `${okr.periodStart}~${okr.periodEnd}`,
  periodStart: okr.periodStart!,
  periodEnd: okr.periodEnd!,
})
const cycleResult = await orchestrator.executeIntent(cycleIntent, MVP_USER_ID)
if (!cycleResult.success || !cycleResult.object) {
  return { success: false, error: `创建周期失败: ${cycleResult.error ?? '未知错误'}` }
}
const cycle = cycleResult.object as Cycle
```

> **注意**：需要将 `createOKROrchestrator` 和 `makeIntent` 从 `okr.ts` 导出（当前是模块私有函数）。将 `createOKROrchestrator` 的 `async function` 改为 `export async function`，`makeIntent` 同理。

- [ ] **Step 2: 导出 createOKROrchestrator 和 makeIntent**

```typescript
// frontend/src/app/actions/okr.ts
// L172: async function createOKROrchestrator() → export async function createOKROrchestrator()
// L216: function makeIntent(...) → export function makeIntent(...)
```

- [ ] **Step 3: 删除 CycleRepository 直接引用**

在 `okr-import.ts` 中删除 `import { CycleRepository } from "@/domains/okrs/repository/cycle"` 和 `const cycleRepo = new CycleRepository()`。

- [ ] **Step 4: 删除 [024.2] 注释块**

删除 L169-175 的「[024.2] 导入创建的周期必须为 in_progress…」注释块。

- [ ] **Step 5: 验证导入流程仍可使用**

```bash
cd frontend && npx vitest run src/domains/okrs/components/__tests__/okr-import-panel.test.tsx 2>&1 | tail -5
```
Expected: 现有 5 个测试 PASS（这些测试不涉及 server action 调用）

- [ ] **Step 6: 验证 tsc**

```bash
cd frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"
```
Expected: baseline 不变

- [ ] **Step 7: Commit**

```bash
git add frontend/src/app/actions/okr.ts frontend/src/app/actions/okr-import.ts
git commit -m "feat(okrs): [022.01] findOrCreateCycle 改走 executeIntent + 退役 [024.2]

- saveImportedOKRs: cycle 创建改走 orchestrator.executeIntent('createCycle')
- 删除 'in_progress' 硬编码（退役 [024.2] 临时修复）
- createOKROrchestrator / makeIntent 导出供 okr-import.ts 复用
- 删除 CycleRepository 直接引用（不再直写）"
```

---

### Task 4: 更新 write-entry-guard 守卫

**Files:**
- Modify: `frontend/src/domains/okrs/__tests__/write-entry-guard.test.ts`

**Interfaces:**
- Consumes: `FILES` 常量, `ALLOWED_DIRECT_WRITES` 白名单
- Produces: 删除 createCycle 例外；新增 `.insert(s.cycles)` 拦截

- [ ] **Step 1: 更新守卫测试**

```typescript
// frontend/src/domains/okrs/__tests__/write-entry-guard.test.ts

const FILES = ['okr.ts', 'okr-import.ts']

/**
 * 例外白名单：空。[022.01] Phase 1 退役 createCycle 旧例外——
 * createCycle 已改走 executeIntent，不再需要直写豁免。
 */
const ALLOWED_DIRECT_WRITES: Array<{ file: string; method: string; reason: string }> = []

describe('[022] OKR 写入口守卫（action 层无 repo 直写）', () => {
  for (const f of FILES) {
    it(`app/actions/${f} 无 .save/.updateProgress/.updateFields 直写`, () => {
      const src = readFileSync(resolve(__dirname, '../../../app/actions', f), 'utf8')
      const code = src
        .replace(/\/\*[\s\S]*?\*\//g, '')
        .split('\n')
        .filter((l) => !l.trim().startsWith('//'))
        .join('\n')

      // allow-list 为空，无需剥离
      expect(code).not.toMatch(/\.(save|updateProgress|updateFields)\s*\(/)
    })

    // [022.01] 新增：.insert(s.cycles) 拦截（精准拦截 findOrCreateCycle 类直写）
    it(`app/actions/${f} 无 .insert(s.cycles) 直写`, () => {
      const src = readFileSync(resolve(__dirname, '../../../app/actions', f), 'utf8')
      const code = src
        .replace(/\/\*[\s\S]*?\*\//g, '')
        .split('\n')
        .filter((l) => !l.trim().startsWith('//'))
        .join('\n')
      expect(code).not.toMatch(/\.insert\(\s*s\.cycles\s*\)/)
    })
  }
})
```

- [ ] **Step 2: 运行守卫测试**

```bash
cd frontend && npx vitest run src/domains/okrs/__tests__/write-entry-guard.test.ts 2>&1 | tail -10
```
Expected: 4 tests PASS（2 files × 2 assertions）

- [ ] **Step 3: Commit**

```bash
git add frontend/src/domains/okrs/__tests__/write-entry-guard.test.ts
git commit -m "feat(okrs): [022.01] write-entry-guard: 删 createCycle 例外 + 扩 .insert(s.cycles) 拦截

- ALLOWED_DIRECT_WRITES 清空（createCycle 已改走 executeIntent）
- 新增 .insert(s.cycles) grep 守卫（per-file，不扩全库）
- 4 测试：2 files × (无 .save 直写 + 无 .insert cycles 直写)"
```

---

### Task 5: manifest 新增 draft→in_progress 转换

**Files:**
- Modify: `frontend/src/domains/okrs/manifest.yaml:139-145`

**Interfaces:**
- Consumes: manifest lifecycle.cycle.transitions
- Produces: 新增 `draft→in_progress (action: start, trigger: intent)` transition

- [ ] **Step 1: 在 manifest 中新增 transition**

```yaml
# frontend/src/domains/okrs/manifest.yaml
# 在 cycle.transitions 中，draft→not_started 之后新增：

      - { from: draft, to: in_progress, trigger: intent, action: start, event_type: CycleStarted }
```

> 插入位置：L141（`draft→not_started`）之后，L142（`not_started→in_progress`）之前。

- [ ] **Step 2: 验证 manifest**

```bash
cd frontend && npm run validate:manifest 2>&1
```
Expected: 0 errors

- [ ] **Step 3: 验证 structure**

```bash
cd frontend && npm run validate:structure 2>&1
```
Expected: pass

- [ ] **Step 4: Commit**

```bash
git add frontend/src/domains/okrs/manifest.yaml
git commit -m "feat(okrs): [022.01] manifest 新增 draft→in_progress (start) 转换

- 允许 draft cycle 直接 start 到 in_progress（跳过 not_started）
- 用于「审核通过」时 now >= periodStart 的场景
- trigger: intent, event_type: CycleStarted"
```

---

### Task 6: CycleCreateDrawer 去 in_progress 硬编码 + draft 文案

**Files:**
- Modify: `frontend/src/domains/okrs/components/cycle-create-drawer.tsx`

**Interfaces:**
- Consumes: `createCycle` (server action, 已改为 executeIntent)
- Produces: 创建 draft cycle + UI 提示文案

- [ ] **Step 1: 移除 status 硬编码**

找到 `cycle-create-drawer.tsx` 中构造 Cycle 对象处（约 L62），将 `status: "in_progress"` 改为 `status: "draft"`。

```typescript
// 旧：
// status: "in_progress",

// 新：
// status: "draft",  // [022.01] SM 强制 draft，不再硬编码 in_progress
```

- [ ] **Step 2: 更新按钮文案与提示**

在提交按钮下方或旁边新增提示文字：

```tsx
{/* 提交按钮后新增提示 */}
<p className="text-xs text-muted-foreground mt-2">
  创建后需「审核通过」才能激活周期，目标将在此期间不可见。
</p>
```

- [ ] **Step 3: 更新 onCreateCycle 调用**

确保 `onCreateCycle` 传递的字段不包含 `status`（由 SM 强制 draft）：

```typescript
// handleSubmit 中
const cycle = await onCreateCycle({
  cycleType: formData.cycleType,
  name: formData.name,
  periodStart: formData.periodStart,
  periodEnd: formData.periodEnd,
  // 不再传 status
})
```

- [ ] **Step 4: 验证组件渲染**

```bash
cd frontend && npx vitest run src/domains/okrs/components/__tests__/ 2>&1 | tail -10
```
Expected: 现有测试 PASS（如有 cycle-create-drawer 相关测试）

- [ ] **Step 5: Commit**

```bash
git add frontend/src/domains/okrs/components/cycle-create-drawer.tsx
git commit -m "feat(okrs): [022.01] CycleCreateDrawer 去 in_progress 硬编码 + draft 提示

- status 改为 'draft'（SM 强制）
- 新增提示文案：创建后需审核通过才能激活
- 不再传 status 字段给 server action"
```

---

### Task 7: 新增「审核通过」菜单项

**Files:**
- Create: `frontend/src/domains/okrs/components/cycle-menu.tsx`
- Create: `frontend/src/domains/okrs/components/__tests__/cycle-menu.test.tsx`

**Interfaces:**
- Consumes: `executeIntent` (via server action)、Cycle 对象
- Produces: `<CycleApproveMenuItem cycle={cycle} onApproved={() => void} />`

- [ ] **Step 1: 写审核通过按钮测试**

```typescript
// frontend/src/domains/okrs/components/__tests__/cycle-menu.test.tsx
/**
 * @file cycle-menu.test
 * @brief [022.01] Phase 1: 审核通过菜单项测试
 */
import { describe, it, expect, vi } from 'vitest'
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'

describe('CycleApproveMenuItem', () => {
  const draftCycle = {
    id: 'cycle-1',
    status: 'draft',
    period: { start: '2026-07-01', end: '2026-09-30' },
    cycleType: 'quarterly',
    name: '2026 Q3',
  }

  it('仅 draft 状态显示「审核通过」菜单项', () => {
    // 非 draft cycle 不显示
  })

  it('点击后弹出二次确认弹窗', async () => {
    // 弹窗含确认/取消按钮
  })

  it('now >= periodStart 时执行 start action', async () => {
    // Mock Date 为 periodStart 之后
  })

  it('now < periodStart 时执行 plan action', async () => {
    // Mock Date 为 periodStart 之前
  })
})
```

- [ ] **Step 2: 运行测试验证失败**

```bash
cd frontend && npx vitest run src/domains/okrs/components/__tests__/cycle-menu.test.tsx 2>&1 | tail -5
```
Expected: FAIL（组件不存在）

- [ ] **Step 3: 创建 server action —— approveCycle**

```typescript
// frontend/src/app/actions/okr.ts 新增
/**
 * 审核通过周期（[022.01] Phase 1）
 *
 * 按 server 时刻 now（UTC date-only）分派：
 * - now >= periodStart → executeIntent('startCycle')（draft → in_progress）
 * - now < periodStart  → executeIntent('planCycle')（draft → not_started）
 *
 * @param cycleId - 周期 ID
 * @returns 执行结果
 */
export async function approveCycle(cycleId: string): Promise<OKRActionResult<Cycle>> {
  try {
    const repo = new CycleRepository()
    const cycle = await repo.findById(cycleId as USOM_ID, MVP_USER_ID)
    if (!cycle) return { success: false, error: '周期不存在' }
    if (cycle.status !== 'draft') return { success: false, error: '仅 draft 状态可审核通过' }

    const now = new Date().toISOString().slice(0, 10) // YYYY-MM-DD
    const action = now >= cycle.period.start ? 'startCycle' : 'planCycle'

    const orchestrator = await createOKROrchestrator()
    const intent = makeIntent(action, { cycleId })
    const result = await orchestrator.executeIntent(intent, MVP_USER_ID)
    if (!result.success) return { success: false, error: result.error }
    return { success: true, data: result.object as Cycle }
  } catch (err) {
    return { success: false, error: err instanceof Error ? err.message : '审核通过失败' }
  }
}
```

> **注意**：`startCycle` 和 `planCycle` action 的命名需与 manifest transition 的 action 字段对应。SM 的 `toStateMachineAction` 将 `startCycle` → `start`、`planCycle` → `plan`。

- [ ] **Step 4: 创建 CycleApproveMenuItem 组件**

```tsx
// frontend/src/domains/okrs/components/cycle-menu.tsx
/**
 * @file cycle-menu
 * @brief [022.01] Phase 1: Cycle 操作菜单（审核通过）
 */
'use client'

import { useState } from 'react'
import { approveCycle } from '@/app/actions/okr'
import {
  Dialog,
  DialogContent,
  DialogDescription,
  DialogFooter,
  DialogHeader,
  DialogTitle,
} from '@/components/ui/dialog'
import { Button } from '@/components/ui/button'

interface CycleApproveMenuItemProps {
  cycle: { id: string; status: string; period: { start: string; end: string } }
  onApproved?: () => void
}

export function CycleApproveMenuItem({ cycle, onApproved }: CycleApproveMenuItemProps) {
  const [open, setOpen] = useState(false)
  const [loading, setLoading] = useState(false)

  if (cycle.status !== 'draft') return null

  const now = new Date().toISOString().slice(0, 10)
  const willStart = now >= cycle.period.start
  const isExpired = now > cycle.period.end

  async function handleApprove() {
    setLoading(true)
    try {
      const result = await approveCycle(cycle.id)
      if (!result.success) {
        alert(result.error ?? '审核失败')
        return
      }
      setOpen(false)
      onApproved?.()
    } finally {
      setLoading(false)
    }
  }

  return (
    <>
      <button
        onClick={() => setOpen(true)}
        className="w-full text-left px-2 py-1.5 text-sm hover:bg-accent rounded"
      >
        审核通过
      </button>

      <Dialog open={open} onOpenChange={setOpen}>
        <DialogContent>
          <DialogHeader>
            <DialogTitle>审核通过此周期？</DialogTitle>
            <DialogDescription>
              {willStart
                ? '审核通过后周期将立即启动，目标变为可见。'
                : '周期尚未到开始日期，审核通过后将进入「未开始」状态。'}
              {isExpired && ' 注意：此周期已过期，审核通过后将自动标记为已结束。'}
            </DialogDescription>
          </DialogHeader>
          <DialogFooter>
            <Button variant="outline" onClick={() => setOpen(false)} disabled={loading}>
              取消
            </Button>
            <Button onClick={handleApprove} disabled={loading}>
              {loading ? '处理中...' : '确认通过'}
            </Button>
          </DialogFooter>
        </DialogContent>
      </Dialog>
    </>
  )
}
```

- [ ] **Step 5: 运行测试验证通过**

```bash
cd frontend && npx vitest run src/domains/okrs/components/__tests__/cycle-menu.test.tsx 2>&1 | tail -10
```
Expected: PASS

- [ ] **Step 6: 集成到 Cycle 菜单中**

在现有 Cycle 操作菜单（⋯ 按钮）中，于 draft cycle 的菜单项列表中添加 `<CycleApproveMenuItem>`。

- [ ] **Step 7: Commit**

```bash
git add frontend/src/domains/okrs/components/cycle-menu.tsx frontend/src/domains/okrs/components/__tests__/cycle-menu.test.tsx frontend/src/app/actions/okr.ts
git commit -m "feat(okrs): [022.01] 审核通过菜单项 + approveCycle server action

- approveCycle: 按 now vs periodStart 分派 startCycle/planCycle
- CycleApproveMenuItem: 仅 draft 显示，二次确认弹窗
- 过期 draft 审核时 toast 提示（isExpired 文案）
- 测试：draft 可见 / 非 draft 隐藏 / 二次确认 / 分派逻辑"
```

---

### Task 8: 端到端验证

**Files:**
- 无新建文件

- [ ] **Step 1: 运行全量 tsc**

```bash
cd frontend && npx tsc --noEmit 2>&1 | tail -5
```
Expected: baseline 错误数不新增（60）

- [ ] **Step 2: 运行全量 vitest**

```bash
cd frontend && npx vitest run 2>&1 | tail -10
```
Expected: 无回归失败

- [ ] **Step 3: 验证 manifest**

```bash
cd frontend && npm run validate:manifest && npm run validate:structure
```
Expected: 0 errors

- [ ] **Step 4: 启动 dev server 验证**

```bash
cd frontend && npm run dev
```
手动验证：
1. 打开 OKR 工作台 → 创建新周期 → 确认创建后为 draft
2. draft cycle ⋯ 菜单出现「审核通过」
3. 审核通过 → cycle 转为 in_progress 或 not_started
4. 导入 OKR 模板 → cycle 为 draft → 审核通过后目标可见

- [ ] **Step 5: Commit 最终状态**

```bash
git add -A
git commit -m "chore(okrs): [022.01] Phase 1 端到端验证通过"
```

---

## 自审清单

1. **Spec 覆盖**：Phase 1 的 6 个子任务（adapter.create / createCycle / findOrCreateCycle / write-entry-guard / manifest / 审核通过）全部覆盖。
2. **无占位符**：所有步骤含完整代码。
3. **类型一致性**：`createCycle` action 名在 okr.ts（`makeIntent('createCycle',...)`）与 manifest（`action: create`）之间通过 `toStateMachineAction` 映射一致。`startCycle`/`planCycle` 同理。
4. **未覆盖的 Phase 1 项**：manifest trigger 类型 `intent+time` 扩展留到 Phase 4 随 sweep 一起做（Phase 1 的 trigger 仍为 `intent`）。

---

**Plan complete and saved to `docs/superpowers/plans/2026-07-02-022-01-phase1-cycle-sm.md`.**

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 1 | CLEAR | 3 issues, 0 critical gaps |

### Decisions

| ID | Issue | Decision |
|----|-------|----------|
| D1 | `createOKROrchestrator` 导出位置 | 提取到 `frontend/src/domains/okrs/wiring.ts`（与 mutation-service.ts 同模式） |
| D2 | Task 1+2 排序问题 | 合并为单 Task：adapter.create + save SET 修复同任务 |
| D3 | createCycle 集成测试 | 新增 `okr-cycle.test.ts`：SM create→draft 黄金路径 + 幂等测试 |

### Required Outputs

**NOT in scope:**
- Phase 2-6（Cycle 权限模型、Obj/KR status 移除、sweep、audit、模板）— 各自后续 PR
- manifest trigger `intent+time` 类型扩展 — Phase 4 随 sweep 一起
- `adapter.cycle.updateStatus` 实现 — Phase 2 需要，Phase 1 仍抛错
- 生产环境 data repair SQL（历史 draft cycle 提升）— 部署前手动执行

**What already exists:**
- `createOKROrchestrator` + `makeIntent` 在 `okr.ts` 中已用于 objective/key_result 创建，Phase 1 复用同一模式
- `resolveObjectType` 按 PascalCase 匹配，`createCycle` action 名正确命中
- `CycleRepository.save` 已有 `onConflictDoUpdate`，仅需修改 SET 子句
- `DomainMutationService.execute` 已接受 `AggregateIntent`，P0-2 路径无需签名变更

**Failure modes:**
- 并发 `adapter.create`：前置 SELECT + onConflictDoUpdate 双重防护，PG READ COMMITTED 下最坏情况返回已有行（不丢数据）
- `createCycle` 的 SM 失败：okr.ts 中 `result.success` 检查 → 返回 `{ success: false, error }`
- `approveCycle` 的 executeIntent 失败：try/catch + error 返回
- 过期 draft cycle 审核：`isExpired` 文案提示，用户可见信号

**Worktree parallelization:** Sequential implementation — all steps touch `okrs/` domain layer and build on each other. No parallelization opportunity.

### Coverage

| Category | Tested | Gaps |
|----------|--------|------|
| Code paths | 11/15 (73%) | createCycle SM 失败路径, saveImportedOKRs executeIntent 替换, 并发创建, approveCycle 失败路径 |
| User flows | 5/11 (45%) | 导入→draft cycle 创建→目标可见, CycleCreateDrawer UX, 过期 draft 审核 toast |

**Verdict: ENG REVIEW CLEARED** — 3 issues all resolved via AskUserQuestion. Plan ready for implementation.

NO UNRESOLVED DECISIONS
