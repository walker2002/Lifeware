# [019.1] CnuiFormAdapter 退役 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 退役被 §IX supersede 的 `CnuiFormAdapter`/`FormRegistry`/`register-form.ts` 死抽象层，`HabitCreationCard` 改 tasks 同款手写范式（顺带接上断掉的 habits create 回填），并新增 validator L4-1/L7-2 前向守卫。

**Architecture:** 迁移本身被代码库强制为唯一干净路径——`HabitForm` 已自带全部默认值 + 已接好 `useServerErrorBackfill`，故 `HabitCreationCard` 只需弃 adapter、直引 `HabitForm`、`serverErrors` 直传。validator 两条规则用现有 TS Compiler API 框架新增纯函数，**先写函数+单测、最后才接入 `main()`**（接入前 adapter 仍在，避免已知债假阳）。

**Tech Stack:** Next.js 16 / React 19 / TypeScript 5 / vitest / 原生 `typescript` Compiler API（validator）

**Spec:** `docs/superpowers/specs/2026-06-22-019.1-cnui-form-adapter-retirement-design.md`

**关键执行约束（来自 memory）：**
- vitest 必须在 `frontend/` cwd 跑（`@/` 映射，repo root 跑会假失败）。
- vitest 不做类型检查，每个任务须配 `npx tsc --noEmit` 双验证；tsc 基线 = **42 错误**，改动文件零新错。
- 失败集用 base/head 对比，不用硬编码计数。
- 所有注释/对话用简体中文；改动的 TS/JS 文件须有 `/** @file ... @brief ... */` 文件头。
- 不得在 `main` 分支直接实现；每任务独立提交。

---

## File Structure

| 文件 | 动作 | 责任 |
|---|---|---|
| `frontend/scripts/validate-domain-structure.ts` | 改 | 新增 `collectSourceFiles` / `findCnuiFormAdapterUsage` / `checkCnuiFormAdapter` / `findFormRegistryRegisterCall` / `checkFormRegistryResidual`；Task 5 接入 `main()` |
| `frontend/scripts/__tests__/validate-domain-structure.test.ts` | 改 | 新增 L4-1/L7-2 纯函数 + 集成测试 |
| `frontend/src/domains/habits/cnui/surfaces/HabitCreationCard.tsx` | 改 | 弃 adapter、直引 `HabitForm`、`serverErrors` 直传 |
| `frontend/src/domains/habits/cnui/__tests__/habit-creation-card.test.tsx` | 增 | 回填直传 + initial + onSubmit 三测 |
| `frontend/src/domains/habits/index.ts` | 改 | 删 `import './register-form'` 副作用 |
| `frontend/src/app/page.tsx` | 改 | 删 `import "@/domains/habits/register-form"` 副作用 |
| `frontend/src/app/actions/intent.ts` | 改 | 删 FormRegistry import + 恒等映射块 |
| `frontend/src/lib/form-registry.ts` | 删 | FormRegistry 抽象 |
| `frontend/src/components/cnui/cnui-form-adapter.tsx` | 删 | CnuiFormAdapter 适配器 |
| `frontend/src/domains/habits/register-form.ts` | 删 | 注册副作用 |
| `docs/domain-development-guide.md` | 改 | 6 处状态翻 ✅ |
| `manifest.md` | 改 | 补版本历史 |

**任务依赖：** Task 1,2（validator 纯函数）独立；Task 3（迁移）独立；Task 4（删引用+删文件）依赖 Task 3；Task 5（接入 main）依赖 Task 4（接入前须先退役，否则对现存 adapter 假阳）；Task 6（文档）独立；Task 7（验收）依赖全部。

---

## Task 1: validator L4-1 纯函数 — `checkCnuiFormAdapter`

**Files:**
- Modify: `frontend/scripts/validate-domain-structure.ts`（新增 `collectSourceFiles` + `findCnuiFormAdapterUsage` + `checkCnuiFormAdapter`）
- Test: `frontend/scripts/__tests__/validate-domain-structure.test.ts`

- [ ] **Step 1: 写失败测试（纯函数 `findCnuiFormAdapterUsage`）**

在 `validate-domain-structure.test.ts` 顶部 import 块追加 `findCnuiFormAdapterUsage, checkCnuiFormAdapter, collectSourceFiles`（从 `'../validate-domain-structure'`）。然后在文件末尾追加：

```ts
// ─── L4-1: checkCnuiFormAdapter 测试 ────────────────────────────

describe('findCnuiFormAdapterUsage（L4-1 纯函数）', () => {
  it('import cnui-form-adapter → 命中', () => {
    const source = sf(`import { CnuiFormAdapter } from '@/components/cnui/cnui-form-adapter'`)
    expect(findCnuiFormAdapterUsage(source)).not.toBeNull()
  })
  it('JSX 标识符 CnuiFormAdapter → 命中', () => {
    const source = sf(`const x = <CnuiFormAdapter domainId="habits" />`)
    expect(findCnuiFormAdapterUsage(source)).not.toBeNull()
  })
  it('无引用 → null', () => {
    const source = sf(`import { HabitForm } from '@/domains/habits/components/habit-form'`)
    expect(findCnuiFormAdapterUsage(source)).toBeNull()
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd frontend && npx vitest run scripts/__tests__/validate-domain-structure.test.ts`
Expected: FAIL — `findCnuiFormAdapterUsage is not a function`（未导出）。

- [ ] **Step 3: 实现 `collectSourceFiles` + `findCnuiFormAdapterUsage` + `checkCnuiFormAdapter`**

在 `validate-domain-structure.ts` 的 `// ─── 常量实例 + CLI ───` 区块**之前**（即 `checkRulesRegistry` 函数之后、`const ROOT_DIR` 之前）插入：

```ts
// ─── [019.1] L4-1：禁 CnuiFormAdapter ──────────────────────────

/**
 * 收集 dir 下所有 .ts/.tsx 源文件（排除 __tests__、.test.*、node_modules、点前缀目录）。
 * 复用 collectActionFiles 的遍历风格，扩展到 .tsx 与全域 src。
 */
export function collectSourceFiles(dir: string): string[] {
  const out: string[] = []
  const walk = (d: string) => {
    let entries: fs.Dirent[]
    try {
      entries = fs.readdirSync(d, { withFileTypes: true })
    } catch {
      return
    }
    for (const e of entries) {
      if (e.name === '__tests__' || e.name === 'node_modules' || e.name.startsWith('.')) continue
      const p = path.join(d, e.name)
      if (e.isDirectory()) {
        walk(p)
      } else if (
        e.isFile() &&
        (e.name.endsWith('.ts') || e.name.endsWith('.tsx')) &&
        !e.name.endsWith('.test.ts') &&
        !e.name.endsWith('.test.tsx')
      ) {
        out.push(p)
      }
    }
  }
  walk(dir)
  return out
}

/**
 * 在单个 sourceFile 中检测 CnuiFormAdapter 残留（import 模块路径含 cnui-form-adapter，
 * 或标识符 CnuiFormAdapter）。返回首个命中位置，便于按文件报一条诊断。
 */
export function findCnuiFormAdapterUsage(sf: ts.SourceFile): { line: number } | null {
  let found: { line: number } | null = null
  const visit = (node: ts.Node) => {
    if (found) return
    if (
      ts.isImportDeclaration(node) &&
      ts.isStringLiteral(node.moduleSpecifier) &&
      node.moduleSpecifier.text.includes('cnui-form-adapter')
    ) {
      const { line } = sf.getLineAndCharacterOfPosition(node.getStart())
      found = { line: line + 1 }
      return
    }
    if (ts.isIdentifier(node) && node.text === 'CnuiFormAdapter') {
      const { line } = sf.getLineAndCharacterOfPosition(node.getStart())
      found = { line: line + 1 }
      return
    }
    ts.forEachChild(node, visit)
  }
  visit(sf)
  return found
}

/**
 * L4-1：扫 src/** 下 CnuiFormAdapter 残留（零豁免——退役后无债务人）。
 * @param srcDir - frontend/src 绝对路径
 */
export function checkCnuiFormAdapter(srcDir: string): Diagnostic[] {
  const diags: Diagnostic[] = []
  for (const f of collectSourceFiles(srcDir)) {
    const content = fs.readFileSync(f, 'utf-8')
    const sf = ts.createSourceFile(f, content, ts.ScriptTarget.Latest, true)
    const hit = findCnuiFormAdapterUsage(sf)
    if (hit) {
      diags.push({
        file: path.relative(srcDir, f),
        level: 'error',
        rule: 'cnui-form-adapter-forbidden',
        message: `第 ${hit.line} 行：禁止使用 CnuiFormAdapter（§IX 已 supersede §CN-UI#4；L4-1）— surface 须手写 + 直接接收 serverErrors（见 domain-development-guide Step 13）`,
      })
    }
  }
  return diags
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd frontend && npx vitest run scripts/__tests__/validate-domain-structure.test.ts`
Expected: PASS（新 3 测全绿，既有测试不退步）。

- [ ] **Step 5: tsc 双验证（中性）**

Run: `cd frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"`
Expected: 42（基线不变；新增纯函数无类型错误）。

- [ ] **Step 6: 提交**

```bash
git add frontend/scripts/validate-domain-structure.ts frontend/scripts/__tests__/validate-domain-structure.test.ts
git commit -m "feat(ci): [019.1] validator L4-1 checkCnuiFormAdapter 纯函数 + 单测

新增 collectSourceFiles/findCnuiFormAdapterUsage/checkCnuiFormAdapter
（TS Compiler API，扫 src/** 禁 CnuiFormAdapter）。
暂未接入 main()——待 Task 5 退役后接入，避免对现存 adapter 假阳。"
```

---

## Task 2: validator L7-2 纯函数 — `checkFormRegistryResidual`

**Files:**
- Modify: `frontend/scripts/validate-domain-structure.ts`（新增 `findFormRegistryRegisterCall` + `checkFormRegistryResidual`）
- Test: `frontend/scripts/__tests__/validate-domain-structure.test.ts`

- [ ] **Step 1: 写失败测试**

import 块追加 `findFormRegistryRegisterCall, checkFormRegistryResidual`。文件末尾追加：

```ts
// ─── L7-2: checkFormRegistryResidual 测试 ──────────────────────

describe('findFormRegistryRegisterCall（L7-2 纯函数）', () => {
  it('FormRegistry.register( 调用 → 命中', () => {
    const source = sf(`FormRegistry.register('habits', 'createHabit', {} as any)`)
    expect(findFormRegistryRegisterCall(source)).not.toBeNull()
  })
  it('FormRegistry.get( 不命中（仅禁 register）', () => {
    const source = sf(`const c = FormRegistry.get('habits', 'createHabit')`)
    expect(findFormRegistryRegisterCall(source)).toBeNull()
  })
  it('无 FormRegistry → null', () => {
    const source = sf(`const x = otherRegistry.register('a')`)
    expect(findFormRegistryRegisterCall(source)).toBeNull()
  })
})

describe('checkFormRegistryResidual（L7-2 文件系统）', () => {
  it('存在 register-form.ts 文件 → 报 form-registry-residual', () => {
    const dir = fs.mkdtempSync(ppath.join(os.tmpdir(), 'src-'))
    fs.mkdirSync(ppath.join(dir, 'domains', 'habits'), { recursive: true })
    fs.writeFileSync(ppath.join(dir, 'domains', 'habits', 'register-form.ts'), 'export {}')
    const diags = checkFormRegistryResidual(dir)
    expect(diags).toHaveLength(1)
    expect(diags[0].rule).toBe('form-registry-residual')
    expect(diags[0].file).toContain('register-form.ts')
    fs.rmSync(dir, { recursive: true, force: true })
  })
  it('含 FormRegistry.register 调用的域文件 → 报', () => {
    const dir = fs.mkdtempSync(ppath.join(os.tmpdir(), 'src-'))
    fs.mkdirSync(ppath.join(dir, 'domains', 'habits'), { recursive: true })
    fs.writeFileSync(
      ppath.join(dir, 'domains', 'habits', 'foo.ts'),
      `FormRegistry.register('habits', 'createHabit', {} as any)`,
    )
    const diags = checkFormRegistryResidual(dir)
    expect(diags).toHaveLength(1)
    expect(diags[0].rule).toBe('form-registry-residual')
    fs.rmSync(dir, { recursive: true, force: true })
  })
  it('干净域 → 零诊断', () => {
    const dir = fs.mkdtempSync(ppath.join(os.tmpdir(), 'src-'))
    fs.mkdirSync(ppath.join(dir, 'domains', 'tasks'), { recursive: true })
    fs.writeFileSync(ppath.join(dir, 'domains', 'tasks', 'surface.ts'), 'export const x = 1')
    expect(checkFormRegistryResidual(dir)).toEqual([])
    fs.rmSync(dir, { recursive: true, force: true })
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd frontend && npx vitest run scripts/__tests__/validate-domain-structure.test.ts`
Expected: FAIL — `findFormRegistryRegisterCall`/`checkFormRegistryResidual` 未导出。

- [ ] **Step 3: 实现 `findFormRegistryRegisterCall` + `checkFormRegistryResidual`**

在 Task 1 新增的 `checkCnuiFormAdapter` 之后插入：

```ts
// ─── [019.1] L7-2：禁 FormRegistry.register + register-form.ts 残留 ─────────

/** 在单个 sourceFile 中检测 FormRegistry.register( 调用，返回首个命中位置。 */
export function findFormRegistryRegisterCall(sf: ts.SourceFile): { line: number } | null {
  let found: { line: number } | null = null
  const visit = (node: ts.Node) => {
    if (found) return
    if (
      ts.isCallExpression(node) &&
      ts.isPropertyAccessExpression(node.expression) &&
      node.expression.expression.getText() === 'FormRegistry' &&
      node.expression.name.text === 'register'
    ) {
      const { line } = sf.getLineAndCharacterOfPosition(node.getStart())
      found = { line: line + 1 }
      return
    }
    ts.forEachChild(node, visit)
  }
  visit(sf)
  return found
}

/**
 * L7-2：扫 src/domains/** 下 FormRegistry.register 调用 + register-form.ts 文件残留（零豁免）。
 * @param srcDir - frontend/src 绝对路径（内部 join 'domains'）
 */
export function checkFormRegistryResidual(srcDir: string): Diagnostic[] {
  const diags: Diagnostic[] = []
  const domainsDir = path.join(srcDir, 'domains')
  for (const f of collectSourceFiles(domainsDir)) {
    if (path.basename(f) === 'register-form.ts') {
      diags.push({
        file: path.relative(srcDir, f),
        level: 'error',
        rule: 'form-registry-residual',
        message: `禁止存在 register-form.ts（§IX 已 supersede；L7-2）— 表单注册中心已退役，surface 须手写`,
      })
      continue
    }
    const content = fs.readFileSync(f, 'utf-8')
    const sf = ts.createSourceFile(f, content, ts.ScriptTarget.Latest, true)
    const hit = findFormRegistryRegisterCall(sf)
    if (hit) {
      diags.push({
        file: path.relative(srcDir, f),
        level: 'error',
        rule: 'form-registry-residual',
        message: `第 ${hit.line} 行：禁止 FormRegistry.register（§IX 已 supersede；L7-2）— 表单注册中心已退役`,
      })
    }
  }
  return diags
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd frontend && npx vitest run scripts/__tests__/validate-domain-structure.test.ts`
Expected: PASS（新 6 测全绿）。

- [ ] **Step 5: tsc 双验证（中性）**

Run: `cd frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"`
Expected: 42。

- [ ] **Step 6: 提交**

```bash
git add frontend/scripts/validate-domain-structure.ts frontend/scripts/__tests__/validate-domain-structure.test.ts
git commit -m "feat(ci): [019.1] validator L7-2 checkFormRegistryResidual 纯函数 + 单测

新增 findFormRegistryRegisterCall/checkFormRegistryResidual
（扫 src/domains/** 禁 FormRegistry.register + register-form.ts 残留）。
暂未接入 main()——待 Task 5 退役后接入。"
```

---

## Task 3: `HabitCreationCard` 手写化（TDD）

**Files:**
- Modify: `frontend/src/domains/habits/cnui/surfaces/HabitCreationCard.tsx`
- Test: `frontend/src/domains/habits/cnui/__tests__/habit-creation-card.test.tsx`

- [ ] **Step 1: 写失败测试**

创建 `frontend/src/domains/habits/cnui/__tests__/habit-creation-card.test.tsx`：

```tsx
/**
 * @file habit-creation-card 单测
 * @brief [019.1] 验证 HabitCreationCard 手写化：serverErrors 直传 HabitForm（接上回填断点）
 */
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { render } from '@testing-library/react'
import { HabitCreationCard } from '../surfaces/HabitCreationCard'

/** 捕获 HabitForm 接收的 props（屏蔽真实表单渲染） */
const captured = { current: null as Record<string, unknown> | null }
vi.mock('@/domains/habits/components/habit-form', () => ({
  HabitForm: (props: Record<string, unknown>) => {
    captured.current = props
    return null
  },
}))

describe('HabitCreationCard [019.1] 手写化', () => {
  beforeEach(() => { captured.current = null })

  it('把 serverErrors 直传给 HabitForm（接上 Lane B 回填断点）', () => {
    render(
      <HabitCreationCard
        surfaceType="habit-creation-card"
        dataModel={{ startDate: '2026-06-22' }}
        onDataChange={() => {}}
        onConfirm={() => {}}
        onCancel={() => {}}
        serverErrors={['标题不能为空']}
      />,
    )
    expect(captured.current?.serverErrors).toEqual(['标题不能为空'])
  })

  it('把 dataModel 作为 initial 传给 HabitForm（默认值由 HabitForm 自身 fallback 提供）', () => {
    render(
      <HabitCreationCard
        surfaceType="habit-creation-card"
        dataModel={{ startDate: '2026-06-22' }}
        onDataChange={() => {}}
        onConfirm={() => {}}
        onCancel={() => {}}
      />,
    )
    expect(captured.current?.initial).toEqual({ startDate: '2026-06-22' })
  })

  it('把 onConfirm 作为 onSubmit 传给 HabitForm', () => {
    const onConfirm = vi.fn()
    render(
      <HabitCreationCard
        surfaceType="habit-creation-card"
        dataModel={{}}
        onDataChange={() => {}}
        onConfirm={onConfirm}
        onCancel={() => {}}
      />,
    )
    expect(captured.current?.onSubmit).toBe(onConfirm)
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd frontend && npx vitest run src/domains/habits/cnui/__tests__/habit-creation-card.test.tsx`
Expected: FAIL — 当前 `HabitCreationCard` 经 `CnuiFormAdapter` 渲染，mock 的 `HabitForm` 永不被调用（`captured.current` 为 null），`serverErrors` 断言失败。

- [ ] **Step 3: 改写 `HabitCreationCard.tsx`**

整文件替换为：

```tsx
/**
 * @file HabitCreationCard
 * @brief 习惯创建卡片 Surface（[019.1] 手写化：弃 CnuiFormAdapter，直引 HabitForm）
 *
 * CNUI Surface 组件——tasks 同款手写范式：直接渲染 HabitForm 并把 serverErrors
 * 直传进去（接 useServerErrorBackfill）。默认值由 HabitForm 自身 useState fallback 提供。
 */
'use client'

import { HabitForm } from '@/domains/habits/components/habit-form'
import type { HabitFormFields } from '@/domains/habits/components/habit-form'

/**
 * 习惯创建卡片属性
 */
interface HabitCreationCardProps {
  /** Surface 类型 */
  surfaceType: string
  /** 数据模型（含服务端 open 返回的初始字段，如 startDate） */
  dataModel: Record<string, unknown>
  /** 数据变更回调（框架契约，当前不逐键回传——与旧 adapter 行为一致） */
  onDataChange: (data: Record<string, unknown>) => void
  /** 确认回调（接收 HabitForm 提交的 HabitFormFields） */
  onConfirm: (data: Record<string, unknown>) => void
  /** 取消回调 */
  onCancel: () => void
  /** 是否加载中 */
  isLoading?: boolean
  /** 是否完成 */
  isDone?: boolean
  /** [019.1] 服务端 submit 失败的 errors，直传 HabitForm 回填 */
  serverErrors?: string[]
}

export function HabitCreationCard({ dataModel, onConfirm, onCancel, isLoading, serverErrors }: HabitCreationCardProps) {
  return (
    <HabitForm
      initial={dataModel as Partial<HabitFormFields>}
      onSubmit={onConfirm}
      onCancel={onCancel}
      isLoading={isLoading}
      serverErrors={serverErrors}
    />
  )
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd frontend && npx vitest run src/domains/habits/cnui/__tests__/habit-creation-card.test.tsx`
Expected: PASS（3 测全绿）。

- [ ] **Step 5: tsc 双验证（中性）**

Run: `cd frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"`
Expected: 42（`HabitCreationCard` 不再 import `CnuiFormAdapter`；adapter 文件仍在但已无此消费者，tsc 不报新错）。

- [ ] **Step 6: 提交**

```bash
git add frontend/src/domains/habits/cnui/surfaces/HabitCreationCard.tsx frontend/src/domains/habits/cnui/__tests__/habit-creation-card.test.tsx
git commit -m "refactor(cnui): [019.1] HabitCreationCard 手写化——弃 CnuiFormAdapter，serverErrors 直传 HabitForm

顺带接上 Lane B 回填断点（旧 adapter 把 serverErrors 渲染成表单下方独立红框、
从不传入 HabitForm）。默认值由 HabitForm 自身 useState fallback 提供（与旧
register-form.ts defaults 逐字相同）。"
```

---

## Task 4: 清理全部引用 + 删除抽象层三件套

**Files:**
- Modify: `frontend/src/domains/habits/index.ts:13`
- Modify: `frontend/src/app/page.tsx:32`
- Modify: `frontend/src/app/actions/intent.ts:33,1319-1329`
- Delete: `frontend/src/lib/form-registry.ts`、`frontend/src/components/cnui/cnui-form-adapter.tsx`、`frontend/src/domains/habits/register-form.ts`

**顺序约束**：先删所有引用（每步后 tree 仍绿），最后删三个文件。

- [ ] **Step 1: 删 `habits/index.ts` 的副作用 import**

`frontend/src/domains/habits/index.ts` 删除这一行：

```ts
import './register-form'
```

- [ ] **Step 2: 删 `app/page.tsx` 的副作用 import**

`frontend/src/app/page.tsx` 删除这一行：

```ts
import "@/domains/habits/register-form"
```

- [ ] **Step 3: 简化 `intent.ts`（删 import + 删恒等映射块）**

`frontend/src/app/actions/intent.ts`：

(a) 删除第 33 行 import：

```ts
import { FormRegistry } from "@/lib/form-registry";
```

(b) 把 `submitCnuiSurface` 中这一段（当前约 1319-1337 行）：

```ts
  // FormRegistry 的字段映射（保留下层映射逻辑）
  const config = FormRegistry.get(domainId, action)
  let mappedFields = fields
  if (config) {
    mappedFields = {}
    for (const [cnuiKey, formKey] of Object.entries(config.fieldMapping)) {
      if (cnuiKey in fields) {
        mappedFields[formKey] = fields[cnuiKey]
      }
    }
  }

  const handler = CNUI_HANDLERS[surfaceType]
  if (!handler) {
    return { success: false, error: `Handler 未找到: ${surfaceType}` }
  }

  // 委托给 domain handler 执行提交
  const result = await handler.submit(action, mappedFields)
```

替换为：

```ts
  const handler = CNUI_HANDLERS[surfaceType]
  if (!handler) {
    return { success: false, error: `Handler 未找到: ${surfaceType}` }
  }

  // 委托给 domain handler 执行提交（[019.1] fieldMapping 恒等已退役，fields 直传）
  const result = await handler.submit(action, fields)
```

- [ ] **Step 4: 删除三个抽象层文件**

Run:
```bash
cd frontend && rm src/lib/form-registry.ts src/components/cnui/cnui-form-adapter.tsx src/domains/habits/register-form.ts
```

- [ ] **Step 5: tsc 验证（中性，且无 dangling import）**

Run: `cd frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"`
Expected: 42（删除后无残留引用；`intent.ts`/`index.ts`/`page.tsx`/`HabitCreationCard` 均已在前序步骤摘除引用）。

- [ ] **Step 6: vitest 回归（handlers.test.ts 不退步）**

Run: `cd frontend && npx vitest run src/domains/habits/cnui/__tests__/handlers.test.ts src/domains/habits/cnui/__tests__/habit-creation-card.test.tsx`
Expected: PASS。

- [ ] **Step 7: 提交**

```bash
git add -A frontend/src
git commit -m "refactor(cnui): [019.1] 删除 CnuiFormAdapter/FormRegistry/register-form 抽象层

- 删 form-registry.ts / cnui-form-adapter.tsx / register-form.ts
- 删 habits/index.ts + app/page.tsx 的 register-form 副作用 import
- intent.ts submitCnuiSurface 去恒等 fieldMapping 映射（mappedFields===fields）

行为保持：fieldMapping 全恒等、defaults 由 HabitForm 自带 fallback。
§IX 已 supersede §CN-UI#4，此抽象层为死代码。"
```

---

## Task 5: validator L4-1/L7-2 接入 `main()` + 真实扫描零命中

**Files:**
- Modify: `frontend/scripts/validate-domain-structure.ts`（`main()` + `SRC_DIR`）
- Modify: `frontend/scripts/__tests__/validate-domain-structure.test.ts`（集成测试）

- [ ] **Step 1: 写集成测试（真实 src 扫描零 L4-1/L7-2 命中）**

在 `validate-domain-structure.test.ts` 的 `import { collectActionFiles, WRITE_ENTRY_EXEMPTIONS } ...` 行改为同时引入 `checkCnuiFormAdapter, checkFormRegistryResidual`：

```ts
import { collectActionFiles, WRITE_ENTRY_EXEMPTIONS, checkCnuiFormAdapter, checkFormRegistryResidual } from '../validate-domain-structure'
```

在文件末尾（最末 `})` 之前）追加新 describe：

```ts
// ─── 集成测试：[019.1] L4-1/L7-2 真实 src 扫描 ────────────────────

const REAL_SRC = ppath.join(FRONTEND, 'src')

describe('integration: [019.1] L4-1/L7-2 真实 src 零残留', () => {
  it('L4-1: src/** 无 CnuiFormAdapter 残留', () => {
    expect(checkCnuiFormAdapter(REAL_SRC)).toEqual([])
  })
  it('L7-2: src/domains/** 无 FormRegistry.register / register-form.ts 残留', () => {
    expect(checkFormRegistryResidual(REAL_SRC)).toEqual([])
  })
})
```

- [ ] **Step 2: 跑集成测试确认通过（退役已完成，应零命中）**

Run: `cd frontend && npx vitest run scripts/__tests__/validate-domain-structure.test.ts`
Expected: PASS（Task 4 已删尽抽象层，真实扫描零命中）。若失败→说明有残留引用，回 Task 4 排查。

- [ ] **Step 3: 接入 `main()`**

在 `validate-domain-structure.ts` 的常量区块（`const DOMAINS_DIR = ...` 之后）追加：

```ts
const SRC_DIR = path.join(ROOT_DIR, 'src')
```

把 `main()` 改为（在 `checkRulesRegistry` 调用后追加两行）：

```ts
function main(): void {
  const diagnostics: Diagnostic[] = []
  for (const f of collectActionFiles(ACTIONS_DIR)) {
    const content = fs.readFileSync(f, 'utf-8')
    diagnostics.push(...analyzeSourceFile(f, content, WRITE_ENTRY_EXEMPTION_SET, ACTIONS_DIR))
  }
  diagnostics.push(...checkRulesRegistry(DOMAINS_DIR, RULES_REGISTRY_EXEMPTION_SET))
  // [019.1] L4-1/L7-2：禁 CnuiFormAdapter + FormRegistry 残留（§IX supersede §CN-UI#4）
  diagnostics.push(...checkCnuiFormAdapter(SRC_DIR))
  diagnostics.push(...checkFormRegistryResidual(SRC_DIR))
  printDiagnostics(diagnostics)
  process.exit(diagnostics.some(d => d.level === 'error') ? 1 : 0)
}
```

- [ ] **Step 4: 端到端跑 validator**

Run: `cd frontend && npm run validate:structure`
Expected: 输出 `✓ validate-domain-structure — 全部通过`，退出码 0（0 错误）。

- [ ] **Step 5: tsc 双验证（中性）**

Run: `cd frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"`
Expected: 42。

- [ ] **Step 6: 提交**

```bash
git add frontend/scripts/validate-domain-structure.ts frontend/scripts/__tests__/validate-domain-structure.test.ts
git commit -m "feat(ci): [019.1] validator L4-1/L7-2 接入 main() + 真实 src 零残留集成测试

退役完成后接入写入口/规则检查流水线：checkCnuiFormAdapter +
checkFormRegistryResidual 对真实 src 扫描零命中。前向守卫——防 re-introduction。"
```

---

## Task 6: 文档同步

**Files:**
- Modify: `docs/domain-development-guide.md`（6 处）
- Modify: `manifest.md`

> 各 Edit 的 old_string 须与文件当前内容逐字匹配；若行号漂移，以文本内容为准定位。

- [ ] **Step 1: guide L115 — validator 行去降级 TODO**

`docs/domain-development-guide.md` 找到：

```
禁 `CnuiFormAdapter`（§IX 已 supersede §CN-UI#4；habits 退役 [019.1] 前降级 TODO——否则对已知债假阳）/禁 `FormRegistry.register`+`register-form.ts` 残留
```

替换为：

```
禁 `CnuiFormAdapter`（§IX 已 supersede §CN-UI#4；[019.1] 已落地，规则 `cnui-form-adapter-forbidden`）/禁 `FormRegistry.register`+`register-form.ts` 残留（规则 `form-registry-residual`）
```

- [ ] **Step 2: guide L144 — L4-1 翻已落地**

找到：

```
| L4-1 | 禁 CnuiFormAdapter（§IX 已 supersede；[019.1] 前降级 TODO） | CI |
```

替换为：

```
| L4-1 | 禁 CnuiFormAdapter（§IX 已 supersede；[019.1] 已落地） | CI |
```

- [ ] **Step 3: guide L152 — L7-2 标已落地**

找到：

```
| L7-2 | 禁 FormRegistry.register/register-form 残留 | CI |
```

替换为：

```
| L7-2 | 禁 FormRegistry.register/register-form 残留（[019.1] 已落地） | CI |
```

- [ ] **Step 4: guide L160 — habits 行 L4 翻 ✅ 手写**

找到 habits 域矩阵行（约 L157-160）：

```
| **habits** | ✅ | ✅ | ✅ | 🟡 CnuiFormAdapter | 🟡 HabitForm | 🟡 | [019.1] 退役 adapter，手写化 |
```

替换为：

```
| **habits** | ✅ | ✅ | ✅ | ✅ 手写 | 🟡 HabitForm | ✅ | [019.1] adapter 已退役、回填已接 |
```

> 注：L6（回填）由 Lane B 接通、Task 3 接上 habits create 断点，翻 ✅；L5（页面 HabitForm 共享）仍 🟡（属"页面表单↔CNUI"技术债，另追踪）。

- [ ] **Step 5: guide L950 — HabitForm 退役分叉注记**

找到：

```
habits 的 `HabitForm`（经 `CnuiFormAdapter`）属待退役分叉（[019.1]）。
```

替换为：

```
habits 的 CNUI 创建 surface 已手写化（[019.1] 退役 `CnuiFormAdapter`，`HabitCreationCard` 直引 `HabitForm`）；页面级 `HabitForm` 与 CNUI 的共享分叉另追踪。
```

- [ ] **Step 6: guide L1730 — Step 13 去降级 TODO 句**

找到：

```
**禁用 `CnuiFormAdapter`**（habits 死抽象）——此禁用**经 §IX（constitution v2.0.0，2026-06-22 生效）已 supersede** §CN-UI 第 4 条（修订记录见 `.specify/amendments/proposed-IX-domain-paradigm.md`，MAJOR，✅ 已生效）。§IX 生效解除宪法层阻塞；但 `L4-1` CI 检查暂续降级 TODO——habits 仍用 `CnuiFormAdapter`，须待 [019.1] habits 手写化后启用（否则对已知债假阳）。
```

替换为：

```
**禁用 `CnuiFormAdapter`**（habits 死抽象）——此禁用**经 §IX（constitution v2.0.0，2026-06-22 生效）已 supersede** §CN-UI 第 4 条（修订记录见 `.specify/amendments/proposed-IX-domain-paradigm.md`，MAJOR，✅ 已生效）。`L4-1`/`L7-2` CI 检查已落地（[019.1]），对真实 src 零残留——作前向守卫防 re-introduction。
```

- [ ] **Step 7: `manifest.md` 补版本历史**

在 `manifest.md` 版本历史表追加一行（日期 2026-06-22，对齐既有行格式）：

```
| 域开发指南 | 2026_06_22 | 2026_06_22 | [019.1] CnuiFormAdapter 退役：habits 手写化 + 删 FormRegistry 抽象层 + validator L4-1/L7-2 落地（§IX 兑现） |
```

> 实际表头列名以 manifest.md 现状为准（如"文档名/更新日期/创建日期/变更说明"），按既有行格式对齐。

- [ ] **Step 8: 提交**

```bash
git add docs/domain-development-guide.md manifest.md
git commit -m "docs: [019.1] guide L4-1/L7-2 翻已落地 + habits L4/L6 翻 ✅ + manifest 版本历史"
```

---

## Task 7: 终态验收门禁

**Files:** 无（仅验证）

- [ ] **Step 1: tsc 基线**

Run: `cd frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"`
Expected: **42**（与 base 一致，改动文件零新错）。

- [ ] **Step 2: vitest 全量（base/head 失败集对比）**

Run: `cd frontend && npx vitest run 2>&1 | tail -40`
Expected: 失败集 = base 失败集（无新增失败）；新增测试（L4-1/L7-2 共 9 测 + HabitCreationCard 3 测 + 集成 2 测）全绿。

- [ ] **Step 3: validator structure**

Run: `cd frontend && npm run validate:structure`
Expected: `✓ validate-domain-structure — 全部通过`，退出码 0。

- [ ] **Step 4: validator manifest（不退步）**

Run: `cd frontend && npm run validate:manifest`
Expected: 与 base 一致（0 error；warning/info 数不增）。

- [ ] **Step 5: 残留 grep 自检**

Run:
```bash
cd frontend && grep -rn "CnuiFormAdapter\|FormRegistry\|register-form\|cnui-form-adapter" src/ --include="*.ts" --include="*.tsx"
```
Expected: **零命中**（三个文件已删，所有引用已清）。

- [ ] **Step 6: 如实报告**

如实报告每步实际输出（不得虚构）。任一门禁未过→不得声明完成，回对应 Task 修复。

---

## Self-Review（plan 自检）

**1. Spec 覆盖**：
- §5.A HabitCreationCard 手写化 → Task 3 ✓
- §5.B 删 3 文件 + 清 2 import → Task 4 Step 1-2,4 ✓
- §5.C intent.ts 去恒等映射 → Task 4 Step 3 ✓
- §5.D validator L4-1/L7-2 → Task 1,2（纯函数）+ Task 5（接入）✓
- §5.E 文档（guide 6 处 + manifest）→ Task 6 ✓
- §6 测试策略（validator TDD + HabitCreationCard 回填测 + 回归 + 门禁基线）→ Task 1-5,7 ✓
- §7 验收门禁 → Task 7 ✓

**2. 占位符扫描**：无 TBD/TODO；每步含完整代码或精确命令 + 期望输出。✓

**3. 类型一致性**：
- `Diagnostic` / `collectSourceFiles` / `findCnuiFormAdapterUsage` / `findFormRegistryRegisterCall` / `checkCnuiFormAdapter` / `checkFormRegistryResidual` 跨 Task 1/2/5 命名一致 ✓
- `HabitCreationCardProps.serverErrors` / `HabitForm` 的 `initial`/`onSubmit`/`serverErrors` 契约跨 Task 3 一致 ✓
- `SRC_DIR` 在 Task 5 定义并在 main 使用 ✓
- validator 测试 import 与导出符号逐字对齐 ✓

无 gap，无类型漂移。
