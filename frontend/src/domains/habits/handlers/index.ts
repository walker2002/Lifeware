/**
 * @file index
 * @brief Habits Domain Handlers 入口
 *
 * 当前 habits 域暂无需要注册的 handler。
 * 所有操作通过 CNUI Surface 的 handlers.ts 直接处理。
 */

/** Habits 域处理器集合（当前为空，操作通过 CNUI Surface handlers 处理） */
export const habitHandlers = {}

export {}
