# Feature Specification: 习惯管理切片

**Feature Branch**: `003-habit-slice`
**Created**: 2026-05-09
**Status**: Draft
**Input**: User description: "初步完成时间盒管理切片后，开始新的功能习惯管理切片，包括习惯库管理、习惯模板、每日计划生成、AI 意图解析、streak 追踪"
**Design Ref**: `docs/superpowers/specs/2026-05-09-habit-management-design.md`

## User Scenarios & Testing

### User Story 1 - 习惯库管理 (Priority: P1)

用户需要定义个人每日习惯及其占用时间。习惯分为可追踪（如运动、复盘，需要打卡和连续天数统计）和纯占时（如用餐、睡眠，只标记时间占用）。每个习惯有默认时间、时间弹性窗口（最早/最晚可安排时间）、默认时长和最小时长。

**Why this priority**: 习惯库是整个习惯管理功能的基础，没有习惯数据就无法支持模板和每日计划生成。独立可用——用户创建习惯后即可在习惯库中查看和管理。

**Independent Test**: 用户可以在习惯库视图中创建、编辑、删除、暂停/恢复习惯，列表按时间排序显示，每个习惯卡片展示完整的时间窗口和分类标记。

**Acceptance Scenarios**:

1. **Given** 用户在习惯库页面, **When** 点击"新建习惯"并填写标题"晨跑"、默认时间 07:00、默认时长 30 分钟、选择"可追踪", **Then** 系统创建习惯，自动推断 earliestTime=06:30、latestEndTime=08:00、minDuration=15，习惯出现在列表中并标记为"可追踪"
2. **Given** 用户有一个"午餐"习惯, **When** 设置 trackable=false、defaultTime=12:00、defaultDuration=45, **Then** 习惯卡片显示"仅占时"标记，无 streak 和完成次数显示
3. **Given** 用户编辑"阅读"习惯的 latestEndTime 从 23:00 改为 22:00, **When** 保存, **Then** 时间窗口可视化条更新，弹性范围收窄
4. **Given** 用户暂停一个 active 习惯, **When** 在习惯库中筛选"已暂停", **Then** 暂停的习惯出现在筛选结果中
5. **Given** 用户删除一个习惯, **When** 该习惯被一个或多个模板引用, **Then** 系统提示该习惯正在模板中使用，要求用户确认删除

---

### User Story 2 - 习惯模板与每日计划 (Priority: P2)

用户可以创建每日习惯模板（如"工作日"、"休息日"），将习惯库中的习惯组装到模板中。每个模板中的习惯可以覆盖默认时间和时长。每日计划时选择模板，系统自动生成时间盒草稿，用户确认后生效。

**Why this priority**: 模板将习惯从"数据"提升为"行动"，是一键生成每日计划的关键机制。依赖 P1 的习惯库数据。

**Independent Test**: 用户创建"工作日"模板，添加 3 个习惯，点击"用模板安排今天"后看到时间轴上的时间盒草稿，调整后确认生效。

**Acceptance Scenarios**:

1. **Given** 用户有 5 个 active 习惯, **When** 创建"工作日"模板并添加"晨跑"（覆盖时间 06:30）、"午餐"、"复盘", **Then** 模板卡片显示 3 个习惯、总时长 1h30m，晨跑显示橙色"覆盖"标记
2. **Given** 用户有一个"工作日"模板, **When** 在模板对比视图中查看, **Then** 时间纵向排列，习惯按时间分布在对应行，未占用时段显示"自由时间"
3. **Given** 用户选择"用工作日模板安排今天", **When** 系统生成时间盒草稿, **Then** 当日时间轴出现 3 个 draft 状态的时间盒，每个通过 timebox_habits 关联对应习惯
4. **Given** 模板生成的时间盒草稿与已有任务时间冲突, **When** 冲突检测运行, **Then** 系统显示冲突提示，用户可选择拖拽调整、压缩时长或跳过
5. **Given** 用户调整完时间盒草稿, **When** 点击确认, **Then** 所有 draft 时间盒变为 planned 状态，习惯本身状态不变

---

### User Story 3 - AI 意图驱动的习惯管理 (Priority: P3)

用户可以通过 AI 助手用自然语言创建习惯、管理模板和生成每日计划。AI 一步生成完整属性并自动推断缺失值，用户只需确认。

**Why this priority**: 增强 AI 交互体验，减少手动操作。依赖 P1 和 P2 的基础设施。

**Independent Test**: 用户在 AI 助手中输入"每天早上7点运动30分钟"，AI 自动创建习惯并展示完整属性供确认。

**Acceptance Scenarios**:

1. **Given** 用户在 AI 助手中输入"每天早上7点运动30分钟", **When** AI 解析意图, **Then** 生成 createHabit 结构化意图，自动推断 trackable=true、earliestTime=06:30、latestEndTime=08:00、minDuration=15
2. **Given** 用户输入"午餐12点，1小时", **When** AI 识别到"午餐"关键词, **Then** 自动推断 trackable=false（用餐类关键词）
3. **Given** 用户输入"创建一个工作日模板", **When** AI 解析, **Then** 生成 createTemplate 意图，applicableDays=[1,2,3,4,5]
4. **Given** 用户输入"用工作日模板安排今天的计划", **When** AI 解析, **Then** 触发 applyTemplate 流程，生成时间盒草稿供确认

---

### User Story 4 - 打卡追踪与 Streak 激励 (Priority: P4)

可追踪习惯支持每日打卡，系统自动计算连续天数（streak）、最长连续天数和 7 天完成次数。达到里程碑时触发激励提醒，连续跳过时触发 streak 保护警告。当可追踪习惯在时间盒中完成打卡后，系统通过 Domain 钩子自动计算并持久化三项指标到习惯记录中。

**Why this priority**: streak 机制是习惯养成的核心驱动力，但可在 P1-P3 稳定后再实现。

**Independent Test**: 用户对"晨跑"习惯打卡，streak 从 6 变为 7，系统显示里程碑成就提醒。

**Acceptance Scenarios**:

1. **Given** "晨跑"习惯 streak=6, **When** 用户今日打卡, **Then** streak 变为 7，系统触发 HabitStreakMilestone 事件，展示 7 天成就激励
2. **Given** "晨跑"习惯连续 3 天跳过, **When** streak > 3, **Then** 系统提升 HabitSkipped 事件权重到 80，显示 streak 保护提醒
3. **Given** "午餐"习惯 trackable=false, **When** 查看待打卡列表, **Then** 午餐不出现在待打卡列表中
4. **Given** 一个可追踪习惯已有连续 3 天打卡记录, **When** 用户今天在时间盒中完成打卡, **Then** 习惯的 streak 更新为 4，longestStreak 取历史最大值，completionRate7d 根据最近 7 天完成次数重新计算
5. **Given** 一个可追踪习惯连续 5 天打卡后中断 1 天, **When** 次日再次打卡, **Then** streak 重置为 1，longestStreak 保持 5 不变
6. **Given** 一个仅占时习惯（trackable=false）, **When** 该习惯在时间盒中被安排执行, **Then** 不触发指标计算逻辑

---

### User Story 5 - 习惯库列表优化 (Priority: P5)

习惯库列表按「可追踪」和「仅占时」分组展示，支持类型+状态组合筛选。每条习惯卡片展示完整信息（标题、描述、状态、是否可追踪、默认时间、时长、频率、连续天数、最长连续、7 天完成数）。草稿和暂停状态可删除（含外键检查），归档习惯变灰。

**Why this priority**: 改善信息浏览效率和数据展示完整度，当习惯数量增多时尤为关键。

**Independent Test**: 创建多个不同类型和状态的习惯，验证分组排序、筛选、卡片信息和按钮可见性。

**Acceptance Scenarios**:

1. **Given** 用户有 3 个可追踪习惯和 2 个仅占时习惯, **When** 打开习惯库, **Then** 列表分为「可追踪」和「仅占时」两组，各组内按默认开始时间从早到晚排序
2. **Given** 用户有 2 个活跃、1 个草稿、1 个暂停的习惯, **When** 在状态筛选中选择「草稿」, **Then** 仅显示 1 个草稿状态的习惯
3. **Given** 用户同时选择「可追踪」和「活跃」筛选, **When** 应用筛选, **Then** 仅显示既是可追踪又是活跃状态的习惯
4. **Given** 一个活跃的可追踪习惯有连续 7 天、最长 12 天记录, **When** 查看卡片, **Then** 显示标题、描述、状态标签、可追踪标签、默认开始时间、默认时长、频率、连续 7 天、最长 12 天、7 天完成次数
5. **Given** 一个草稿习惯无关联数据, **When** 点击「删除」按钮, **Then** 弹出确认对话框，确认后执行删除
6. **Given** 一个暂停习惯已有打卡记录关联, **When** 点击「删除」按钮, **Then** 弹出确认对话框，确认后系统检查外键引用，存在引用则提示无法删除
7. **Given** 一个归档习惯, **When** 查看卡片, **Then** 卡片颜色变灰，不显示暂停或删除按钮
8. **Given** 习惯数量超出屏幕高度, **When** 查看列表, **Then** 列表区域显示滚动条

---

### Edge Cases

- 睡眠习惯跨午夜（22:00-06:00），时间比较逻辑如何处理？
- 用户删除一个被模板引用的习惯时如何处理？
- 模板内习惯的时间覆盖超出原习惯的 earliestTime~latestEndTime 范围时如何校验？
- 同一天多次应用模板，是否覆盖之前的 draft 时间盒？
- 习惯的 minDuration 压缩后仍无法安排时，系统建议取消还是跳过？
- 一个时间盒同时绑定两个习惯（如午餐+听书）时，打卡如何处理？
- 习惯刚创建（草稿状态）尚无打卡记录时，三个指标均显示 0
- 同一天对同一习惯多次打卡（数据库有唯一约束，只保留最后一条）
- 最近 7 天完成次数基于自然日（今天往前推 7 天），非滚动 168 小时
- 所有习惯都处于归档状态时，列表显示空状态提示
- 习惯卡片在一行排列时，不同屏幕宽度下每行显示的卡片数量自适应
- 新建模板时，若当前无活跃习惯，模板中习惯列表为空，用户可手动逐个添加

---

### User Story 6 - 习惯库卡片布局与交互优化 (Priority: P6)

习惯卡片采用固定宽度、多列网格布局，一行可显示多个卡片，页面紧凑不浪费空间。当前系统缺少「激活」按钮，草稿状态的习惯需要有明确的「激活」操作才能转为活跃状态。删除操作需要二次确认对话框。

**Why this priority**: 当前卡片独占一行造成信息密度低、页面松散。删除缺少确认容易误操作。缺少激活按钮导致习惯无法从草稿转入活跃状态。

**Independent Test**: 创建 5+ 个习惯，验证网格布局在不同窗口宽度下自适应排列。激活一个草稿习惯，验证状态变为活跃。删除一个习惯时验证确认对话框。

**Acceptance Scenarios**:

1. **Given** 用户有 5 个习惯, **When** 打开习惯库, **Then** 卡片以网格形式排列，一行显示多个卡片（非独占一行），页面紧凑
2. **Given** 习惯库在不同浏览器窗口宽度下, **When** 调整窗口大小, **Then** 每行卡片数量自适应（宽屏显示更多，窄屏显示更少），卡片固定宽度不拉伸
3. **Given** 习惯创建后状态为草稿, **When** 查看习惯卡片, **Then** 卡片显示「激活」按钮（将草稿转为活跃状态）
4. **Given** 用户点击草稿习惯的「激活」按钮, **When** 激活成功, **Then** 习惯状态变为活跃，卡片按钮变为「暂停」（不再显示「激活」）
5. **Given** 用户点击习惯卡片的「删除」按钮, **When** 删除请求触发, **Then** 弹出确认对话框显示「确定要删除该习惯吗？此操作不可撤销」，用户确认后才执行删除
6. **Given** 用户点击删除后弹出确认对话框, **When** 用户点击「取消」, **Then** 对话框关闭，习惯不被删除

---

### User Story 7 - 习惯模板编辑与删除 (Priority: P7)

已创建的习惯模板支持编辑（修改名称、适用日、习惯列表）和删除（含二次确认）。新建模板时自动添加所有当前活跃习惯，用户可修改时间和移除习惯。

**Why this priority**: 模板缺少编辑和删除是功能缺失，影响日常使用效率。自动填充活跃习惯减少手动操作。

**Independent Test**: 创建一个模板，验证可以编辑模板内容、删除模板（含确认）。新建模板时验证活跃习惯自动填充。

**Acceptance Scenarios**:

1. **Given** 用户有一个已创建的「工作日」模板, **When** 点击模板的「编辑」按钮, **Then** 进入编辑页面，可修改名称、适用日、习惯列表中的时间覆盖
2. **Given** 用户在编辑模板页面, **When** 修改习惯的开始时间并保存, **Then** 模板更新成功，覆盖后的时间在模板视图中正确显示
3. **Given** 用户在编辑模板页面, **When** 点击移除某个习惯, **Then** 该习惯从模板中移除，但不影响习惯库中的原习惯
4. **Given** 用户点击模板的「删除」按钮, **When** 删除请求触发, **Then** 弹出确认对话框「确定要删除该模板吗？此操作不可撤销」，用户确认后删除
5. **Given** 用户点击新建模板, **When** 进入新建模板页面, **Then** 系统自动添加所有当前活跃习惯到模板中，按默认开始时间排序，每个习惯的开始时间等于其默认开始时间
6. **Given** 新建模板页面已自动填充活跃习惯, **When** 用户修改某个习惯的开始时间, **Then** 该习惯的时间覆盖被更新，不影响习惯库中的默认时间
7. **Given** 当前没有任何活跃习惯, **When** 新建模板, **Then** 模板中习惯列表为空，用户可手动逐个添加

## Requirements

### Functional Requirements

- **FR-001**: 系统 MUST 支持创建习惯，包含标题、描述、默认时间、时间弹性窗口（earliestTime/latestEndTime）、默认时长、最小时长、是否可追踪、频率类型
- **FR-002**: 系统 MUST 为习惯提供 `trackable` 属性，可追踪习惯支持打卡和 streak 统计，纯占时习惯仅标记时间占用
- **FR-003**: 系统 MUST 支持习惯的时间弹性窗口：每个习惯有 defaultTime（默认时间锚点）、earliestTime（最早可安排）、latestEndTime（最晚须结束）
- **FR-004**: 系统 MUST 支持习惯时长压缩：每个习惯有 defaultDuration（默认时长）和 minDuration（最小时长），压缩不低于 minDuration
- **FR-005**: 系统 MUST 支持习惯的完整生命周期：draft → active → suspended → archived，及对应的状态转换
- **FR-006**: 系统 MUST 支持创建习惯模板，模板引用习惯库中的习惯（外键关联），可选择性覆盖时间和时长
- **FR-007**: 系统 MUST 支持模板的适用日设置（applicableDays），指定模板适用于星期几
- **FR-008**: 系统 MUST 支持通过模板一键生成每日时间盒计划：遍历模板中的习惯，在当日时间轴创建 draft 时间盒，通过 timebox_habits 关联
- **FR-009**: 系统 MUST 在模板生成时间盒时进行冲突检测，包括习惯之间、习惯与已有任务/时间盒之间的时间重叠
- **FR-010**: 系统 MUST 允许用户在确认前调整时间盒草稿：拖拽调整时间、压缩时长、跳过某个习惯
- **FR-011**: 系统 MUST 支持 AI 自然语言创建习惯，一步生成完整属性并自动推断缺失值（earliestTime、latestEndTime、minDuration、trackable）
- **FR-012**: 系统 MUST 支持 AI 自然语言管理模板（创建模板、添加习惯到模板、用模板安排计划）
- **FR-013**: 系统 MUST 对可追踪习惯支持打卡，记录 completed/skipped/partial 状态和实际耗时
- **FR-014**: 系统 MUST 自动计算可追踪习惯的 streak（连续天数）、最长 streak 和 7 天完成次数
- **FR-015**: 系统 MUST 在 streak 达到里程碑（7/14/30 天）时触发激励事件，在连续跳过超过阈值时触发保护警告
- **FR-016**: 系统处理跨午夜习惯时，当 latestEndTime < earliestTime 视为跨日（如睡眠 22:00-06:00）
- **FR-017**: 当可追踪习惯在时间盒中完成打卡后，系统 MUST 通过 Domain 钩子自动重新计算 streak、longestStreak 和 completionRate7d 三项指标
- **FR-018**: 上述三项指标 MUST 持久化到习惯记录中，而非每次查询时实时计算
- **FR-019**: 仅占时习惯（trackable=false）不触发指标计算
- **FR-020**: 习惯库列表 MUST 按「可追踪」和「仅占时」分组展示，组内按默认开始时间排序
- **FR-021**: 列表顶部 MUST 提供类型筛选（全部 / 可追踪 / 仅占时）和状态筛选（全部 / 草稿 / 活跃 / 暂停 / 归档），两个维度可组合
- **FR-022**: 每条习惯卡片 MUST 展示：标题、描述、状态标签、是否可追踪、默认开始时间、默认时长、频率信息、当前连续天数、历史最长连续、最近 7 天完成次数
- **FR-023**: 归档习惯卡片颜色变灰，不显示暂停或删除按钮
- **FR-024**: 草稿和暂停状态的习惯卡片显示「删除」按钮，点击时弹出确认对话框；确认后检查外键引用（habit_logs、timebox_habits、template_habits），存在引用则拒绝删除并提示原因
- **FR-025**: 习惯列表内容超出屏幕时 MUST 显示滚动条
- **FR-026**: 习惯卡片 MUST 采用固定宽度的网格布局，一行显示多个卡片，页面紧凑不浪费空间
- **FR-027**: 卡片网格 MUST 响应式自适应，宽屏每行显示更多卡片，窄屏减少卡片数
- **FR-028**: 习惯卡片 MUST NOT 显示独立的「激活」按钮（草稿状态即视为可用）
- **FR-029**: 删除习惯 MUST 弹出确认对话框，用户确认后才执行删除；取消则关闭对话框不执行
- **FR-030**: 已创建的习惯模板 MUST 支持编辑功能：修改名称、适用日、习惯列表中的时间覆盖
- **FR-031**: 模板编辑页面 MUST 支持移除习惯（仅解除模板关联，不影响习惯库中的原习惯）
- **FR-032**: 删除习惯模板 MUST 弹出确认对话框，用户确认后才执行删除
- **FR-033**: 新建模板时 MUST 自动添加所有当前活跃习惯到模板中，按默认开始时间排序，每个习惯的开始时间设为该习惯的 defaultTime；用户可修改时间覆盖或移除习惯

### Key Entities

- **Habit（习惯）**: 核心实体，包含标题、时间模型（defaultTime/earliestTime/latestEndTime）、时长模型（defaultDuration/minDuration）、trackable 分类、频率、生命周期状态、streak 统计数据
- **HabitTemplate（习惯模板）**: 场景化的习惯编排方案，包含名称、适用日、引用的习惯列表
- **TemplateHabit（模板习惯关联）**: 模板与习惯的引用关系，支持时间和时长的选择性覆盖
- **HabitLog（打卡记录）**: 可追踪习惯的每日打卡数据，包含状态、实际耗时、备注

## Success Criteria

### Measurable Outcomes

- **SC-001**: 用户能在 30 秒内通过 UI 创建一个新习惯，系统自动补全所有时间弹性参数
- **SC-002**: 用户能在 10 秒内通过 AI 助手创建一个新习惯，只需一句自然语言描述并确认
- **SC-003**: 用户能在 5 秒内通过模板一键生成每日时间盒计划，冲突项少于 20% 的日常场景无需手动调整
- **SC-004**: 习惯库中的习惯能正确反映到模板视图和每日时间盒中，数据一致性 100%
- **SC-005**: 可追踪习惯的 streak 计算准确率 100%，里程碑提醒在打卡后即时触发
- **SC-006**: 模板视图与时间盒 timeline 方向一致（纵向时间轴），用户在两个视图间切换无认知负担
- **SC-007**: 打卡完成后三项指标（streak、longestStreak、completionRate7d）在 1 秒内完成更新
- **SC-008**: 用户可通过类型+状态两个维度组合筛选习惯，筛选结果准确无遗漏
- **SC-009**: 每条习惯卡片展示 10 项信息，用户无需点击即可概览习惯全貌
- **SC-010**: 删除有外键引用的习惯时，系统 100% 阻止误删并给出明确提示
- **SC-011**: 习惯卡片以网格布局排列，10+ 个习惯无需滚动即可概览（紧凑排列）
- **SC-012**: 删除习惯和模板时，100% 经过确认对话框，无直接删除路径
- **SC-013**: 新建模板自动填充活跃习惯，用户无需手动逐个添加，3 秒内可完成模板创建

## Assumptions

- 现有时间盒管理系统已完成（002-timebox-slice），习惯通过 timebox_habits junction table 关联时间盒
- 现有数据库 schema 中 habits 表已定义，需要扩展字段和重命名部分字段
- 现有 USOM 类型中 Habit/HabitLog/HabitSummary 已定义，需要扩展
- habits domain 插件目录已存在但为空，需要实现四钩子接口
- 冲突仲裁矩阵中习惯相关规则（C-03 ~ C-07）已在方法论文档中定义，需要实现
- AI 意图解析基于现有 ai-parser.ts 框架扩展
- MVP 阶段仅 Web 端，移动端后续迭代
- Phase 1 先实现习惯库基础，Phase 2 实现模板和每日计划，Phase 3 实现 AI 和 streak
- 打卡记录的"连续"定义基于自然日：若某天无 completed 记录，连续即中断，次日打卡 streak 从 1 重新计算
- 最近 7 天完成次数统计 completed 状态的记录数，skipped 和 partial 不计入
- completionRate7d 存储最近 7 天内的完成次数（绝对值），非百分比
- 指标更新由 Domain 插件的 onEvent 钩子在 HabitLogged 事件时触发计算
- 已归档习惯在列表中仍然可见（颜色变灰），不隐藏
- 习惯卡片固定宽度（约 280-320px），使用 CSS Grid 或 Flexbox wrap 自动换行
- 草稿习惯不需要用户手动激活——创建即可用于模板和时间盒安排
- 删除确认对话框使用 AlertDialog 组件，与归档确认对话框风格一致
- 模板编辑复用新建模板的表单组件，区别仅在于 initial 数据预填充
- 新建模板自动填充的活跃习惯不包含已暂停或已归档的习惯
