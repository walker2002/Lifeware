

``` 
文档说明：
- 本文档描述当前正在做的待澄清需求
- AI需要通过读取该文档完成需求规划
- 状态为“已完成”的是已完成的内容，无需处理
- 由开发者人工来维护“状态”栏的内容
```



## [001] 修正Manifest.yaml 的运行时参考机制

### 状态

完成时间：20260516



### 现状

之前已经完成了 针对 Domain manifest.yaml 的更新，但是检查过程中，发现manifest.yaml 的内容还是被部分硬编码，因此需要强调一个重要规范：

**manifest.yaml 的主体应该是运行时配置，不是开发时文档。**

目前已更新宪章文件



### 修改要求

- 请检查 当前已实现的 4个Domain，找到硬编码的内容，替换成运行时模式
- 为了避免 manifest.yaml 的意外错误编辑导致系统运行问题，在运行时读取数据时，需要增加检测 .yaml 文件语法是否合规的处理
