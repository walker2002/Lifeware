``` 
文档说明：
- 本文档描述当前正在做的待澄清需求
- AI需要通过读取该文档完成需求规划
- 状态为“已完成”的是已完成的内容，无需处理
- 由开发者人工来维护“状态”栏的内容
```





## OKR 信息导入

### 状态

- 完成时间：

### [000] OKR信息的智能提取和导入功能

现在开始开发一个新功能：在OKR管理中，增加从文件读取并整理生成 OKR 的功能：

- 设计一套基于 USOM 中 OKR的规范 Markdown 文档，可供用户下载并按照格式填写数据
- 在OKR“新建”按钮旁边，增加“文件导入”、“模板下载”功能
- “文件导入”功能可上传文件，包括Markdown/excel/word/txt，文档格式不一定符合模板要求，需要启用 AI 从中有效识别OKR的元素并提取出来，同时写入规范点的Markdown文档中。
- 形成简要的提取报告：如果大部分数据无法识别，告诉用户还需要提供哪些信息，如果核心数据可提取，则告知用户需补充哪些信息
- 提取完成后，主显示区显示可编辑的Markdown文件（符合模板标准的），由用户现场修改
- 用户选择“保存”，则所有OKR存入数据库，状态是“草稿”，由用户逐个激活

